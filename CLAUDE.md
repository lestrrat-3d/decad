# CLAUDE.md

Guidance for working in this repository. Read before making structural changes.
Update when a design variable gets resolved.

## What this is

A **headless CAD engine** in Go — the 3D modeling layer above the `sketch` 2D
constraint engine. Build solids in code, then interrogate them programmatically.

**North-star use case:** a *headless 3D verification oracle*. A coding agent
models a part here and proves it sound — watertight, correct volume, no
interference, no wall thinner than the tool — BEFORE committing to write real
CAD software code (e.g. an Autodesk Fusion add-in). Be wrong in the cheap place.

**Current state: the public API is landing incrementally against an approved
design.** What the package exports today is the leading edge of that surface;
everything it does not yet export remains design-only, and every capability the
design consumes exists in its dependencies — there is no open dependency gap.
`docs/api-design.md` is the core contract for the whole surface: a
recipe/evaluator split, a B-rep-shaped surface, immediate-mode features,
selectors instead of handles, and `Exactness` on every measurement. Two
companion designs carry its deep ends: `docs/sketch-seam-design.md` (the trim
contract and recording IR at the `sketch` seam) and
`docs/verification-design.md` (the tolerance gate and noise floor). Read them
before writing any public type. `docs/evaluator-design.md` is the v1
evaluator's own design — topology construction, mass properties, staging
(`ErrUnsupported`), the mesh boolean, the Verify implementation; read it
before writing evaluator/topology/feature code.

## Hard rules

- **Layering is `decad -> sketch -> r3 -> units`.** decad imports all three
  directly. NEVER import decad from any of them; they do not know it exists.
- **NEVER re-derive a 2D answer.** Profile closure, DOF, constraint conflicts,
  sketch validity, an intersection, a cut parameter, a projection onto a curve →
  ask `sketch`, consume its answer. Where `sketch` reports its own answer
  approximate — a `Partial` fragment whose cut is sampled, `BoundaryEdge.TExact`
  false (`docs/sketch-seam-design.md`) — decad **rejects**. It never repairs,
  projects, fits, or infers the exact answer. A whole (non-`Partial`) edge
  records from the entity's own data and never consults `TExact`.
- **A decad-side check may only FALSIFY an upstream claim, never bless one.**
  Admission is decided by what `sketch` says — `BoundaryEdge.TExact` for a
  `Partial` fragment — never by a test decad runs on the
  geometry it was handed. A residual against a source curve is admissible in exactly
  one direction: **large ⇒ the claim is disproven ⇒ reject**; **small ⇒ proves
  nothing** — a sampled cut can lie arbitrarily close to the curve, so a small
  residual NEVER admits an input (`docs/sketch-seam-design.md`). A check that can accept
  is an admission gate, and an admission gate on a residual is unsound. Reject-only,
  always.
- **NEVER hand-roll coordinate math.** Vectors, frames, local↔world transforms →
  `r3`. Its `Frame` is orthonormal, so the inverse is the transpose, never a
  matrix solve.
- **Shapes belong HERE.** `r3` excludes them by charter; solids/surfaces/meshes/
  topology are this module's job.
- **NEVER add a public API that contradicts the design docs** —
  `docs/api-design.md` and its companions `docs/sketch-seam-design.md` and
  `docs/verification-design.md`. Extending them is fine; changing a decision
  means changing the doc first.
- **NEVER expose triangles as the representation, indices as selectors, or a bare
  `float64` measurement. NEVER give a boolean a target-out parameter or let it
  mutate an operand.** These are the forward-compatibility invariants that keep
  an exact-kernel future reachable (`docs/api-design.md` §3). Scalar quantities —
  values and their error bounds alike — are `units.Value`. Exactly two things are
  not scalar quantities and so fall outside the rule (`docs/api-design.md` §5.2):
  the **coordinate** — an `r3.Vec`, or a plane-local `Point2` — which is a length in
  millimetres by convention; and the **curve parameter** — a spline's degree, knots
  and weights, a recorded segment's parameter range (`TStart`/`TEnd`), a conic's
  fullness `Rho` — which is a dimensionless index into a parameterisation, not a
  measurement of anything.
  Neither is a licence for a bare float anywhere else.
- **NEVER add a `go.mod` module without recording the decision here.** Approved:
  - `github.com/lestrrat-3d/sketch` — parametric 2D constraint engine.
  - `github.com/lestrrat-3d/r3` — 3D coordinate math (`Vec`, `Frame`,
    `Transform`).
  - `github.com/lestrrat-3d/units` — typed quantities (`Value`, `Kind`).
    Direct: decad's `Measurement` and `Recipe` quantities are `units.Value`.
    It is the same module `sketch` uses for its dimensions (`sketch` has no
    in-tree units package), so there is no parallel unit system to reconcile.
  - `github.com/lestrrat-go/option/v3` — functional options (house library). Used
    by feature options.
  - `github.com/stretchr/testify/require` — assertions, **test code only**.
    NEVER import from production code.
- **Correctness must be observable.** Every capability ships with a test
  asserting on computed geometry (coordinates, volumes, residuals) — NEVER
  merely "it ran".

## Layout

| Path | Responsibility |
|---|---|
| `docs/api-design.md` | **The core public API contract.** Recipe/evaluator split, forward-compat invariants, feature + selector + verification surface. Points at the two companion designs. |
| `docs/sketch-seam-design.md` | The recording contract at the `sketch` seam: trim contract (`TExact`), the `CurveSegment` recording IR, `ErrUnrecordableProfile`. |
| `docs/verification-design.md` | How `Verify` judges every bounded result: report + statuses, `WithTolerance`, the diameter-anchored noise floor. |
| `docs/evaluator-design.md` | The v1 evaluator: evaluate-from-the-record rule, topology model + provenance roles, mass properties on decad's own records, per-feature build tables, explicit staging via `ErrUnsupported`, the exact-predicate mesh boolean, Verify's implementation, the increment plan. |
| `docs/clearance-design.md` | The increment-3 clearance kernel: how a minimum gap between two analytic bodies is proven — the stationarity-tier candidate enumeration with exact trim admission, the face-pair distance table (closed-form / certified polynomial bracket / branch-and-bound), the disjointness proof (boundary clearance + nesting exclusion), touching-contact classification, and how rows feed the report. |
| `doc.go` | Package doc: scope + the layering contract. |
| `errors.go` | The core §12 sentinel error vocabulary — one branchable identity per case an agent must branch on. |
| `measurement.go` | The bounded-result shapes (core §5.3/§6): `Exactness`, `Measurement`, `VecMeasurement`, `Box`. |
| `record.go` | The recording IR (seam §2): `PlaneRecord`, `Point2`, `ProfileRecord`, `LoopRecord`, the ten sealed `CurveSegment` variants, and their tagged JSON codec (pointer variants normalize to values; nil pointers and unknown/missing kind tags are rejected). |
| `seam.go` | The seam conversion: `RecordProfile(s, p)` — the §7 gates (`ErrForeignProfile`/`ErrStaleProfile`/`ErrInvalidProfile`), the `TExact` admission gate, and the reject-only falsifier (`falsifyRange`, evaluating through `sketch/geom`'s own evaluators — the only `Polyline` read, endpoints only, check-never-record). |
| `extent.go` | The extent vocabulary (core §8.1): `Direction` (named text codec), the sealed linear `Extent`/`SideExtent` tiers (`Distance`/`ThroughAll`/`Symmetric`/`TwoSided`; `DistanceSide`/`ThroughAllSide`) and the sealed angular `AngularExtent`/`SideAngular` tiers (`AngleExtent`/`FullRevolution`/`SymmetricAngle`/`TwoSidedAngle`; `AngleSide`), each with its own tagged codec — the two sets are disjoint in the type system and on the wire. The body-relative stops `ToFace` (both `Extent` AND `SideExtent`; `Offset` the one SIGNED number in the vocabulary — positive overshoots the face, the zero Value means none) and `ToFaceAngular` (both `AngularExtent` AND `SideAngular`, no offset) are core §8.1's deliberate dual-tier exceptions: their codecs record the body as its producing `StepRef` (a live `*Body` never encodes, like `EdgeAxis`), decode through pointer wire fields, reject nil variant pointers (wrap `ErrDegenerate`), and the clone helpers (`cloneExtent`/`cloneAngularExtent`) deep-copy their selectors so no caller-owned query survives into a recorded step or escapes `Recipe()`. |
| `recipe.go` | The Recipe IR (core §6.2): `Recipe`/`Step`/`StepRef`/`OpKind` (named text codec)/`BodyRef`/`Selector` root/`StepOpts` (`ExtrudeOpts`), and Step's wire codec — absent fields omitted, every sealed field (selectors included) dispatched through its closed-set codec, the extent/angular one-of keying enforced on both wire directions. |
| `selector.go` | The selector vocabulary (core §9): `EdgeSelector`/`FaceSelector`, the `*EdgeQuery`/`*FaceQuery` variants of `Selector` (predicate conjunction + `Exactly`/`AtLeast` cardinality), the `EdgePredicate`/`FacePredicate` constructors (`Convex`/`Concave`/`ParallelTo`/`LongerThan`/`CreatedBy`/`Circular`; `Planar`/`Cylindrical`/`NormalTo`/`FaceCreatedBy`), and their tagged codec. The variants seal in with POINTER receivers, so there is no value form to normalize to: nil pointers are rejected (`errNilSelector` wraps `ErrDegenerate`) and the clone helpers deep-copy — no caller-owned pointer survives into a recorded step, none escapes `Recipe()`. Resolution (`SelectEdges`/`SelectFaces`, evaluator §7) is the filter pipeline over the body's live topology: gather (`Body.Edges()`/`Faces()`, whose order the result keeps — deterministic, so a recipe replay selects identically), validate predicate parameters up front (zero direction `ErrDegenerate`, non-finite `ErrNotFinite`, `LongerThan` non-length/negative `ErrUnitKind`/`ErrNegativeMagnitude`), filter by conjunction, then enforce cardinality (`Exactly`/`AtLeast` miss is `ErrCardinality` even at zero matches; unasserted zero is `ErrNoMatch` — core §12 precedence). Matching is decided on what an entity IS: `ParallelTo` compares a LINEAR edge's start→end direction (either sense; a curved edge never matches), `NormalTo` a PLANAR face's plane normal (either sense), `LongerThan` is strict on `Edge.Length()`, `Circular` means `Circle3`/`Arc3`, `Convex`/`Concave` read the decided `IsConvex` (the walked-boundary convention — `Concave` is what picks a hole's rim), `FaceCreatedBy` matches ANY of the face's multi-role `Origins()`, and `CreatedBy` matches an edge through its adjacent faces' `Origins()` (an edge carries no roles of its own). Resolution reads only the public topology, never the payload — prism and revolve bodies alike. |
| `moments.go` | The mass-property engine (evaluator §4): exact Green's-theorem boundary integrals over a `ProfileRecord` — `Area()` (a `Measurement`, Kind Area, Exact/zero-bound) and `Centroid()` (a `VecMeasurement` whose Value is PLANE-LOCAL (u, v, 0) mm) — plus `SecondMoments()` (∫u²dA/∫uv dA/∫v²dA about the plane origin, Kind SecondMomentOfArea — what a revolve centroid needs) — closed forms for line/circle/arc walks incl. pointer variants; a `CircleSeg` whose `CCW` contradicts its range order is `ErrDegenerate`; free-form kinds are `ErrUnsupported` until their increments. |
| `topology.go` | The topology model (evaluator §3): `Body`→`Lump`→`Shell`→`Face`→`Loop`→`Edge`→`Vertex` with the core §6/§6.1 accessors, sealed `Surface` (`Plane`/`Cylinder`/`Cone`/`Sphere`/`Torus`/`Faceted` — the honest boolean-output flag, carrying its proven chord `Bound`; `SurfaceKind` discriminants) and `Curve` (`Line3`/`Circle3`/`Arc3`/`FacetedCurve`) sets, multi-role `Face.Origins()`, bounded `Vertex.Position()`. `Edge.IsConvex` is the WALKED-BOUNDARY convexity, never the 3D material angle: the profile is walked with the material on its left (outer loop CCW, holes CW), a junction edge reads the turn between its two walls, a rim edge reads the sense of the wall it runs along (a circular wall by its own turn — CCW convex, CW concave — a straight wall by its loop's role), and a partial revolve's on-axis cap edge is convex under a half turn — so a HOLE's rim edges are concave, as are those of a concave round on the outer loop, even though the material there is a chamferable quarter-turn wedge. `Edge.Length()`/`Face.Area()` key exactness off their own proven bound: zero for the analytic set, the composed bound on a boolean-built body — a float-computed length or area is never Exact. Whether a face is FLAT is `Face.isPlanar()`, not the surface tag: an analytic `Plane` is by construction, and a `Faceted` face is when every source it descends from was, so a second-generation all-planar boolean's straight rims answer instead of refusing (a `Faceted` rim on a genuinely CURVED source still refuses — its true length is unboundable). Bodies are immutable after construction. |
| `document.go` | `Document` (`New`/`Bodies`/`Recipe`), the atomic commit tail (record→evaluate→commit; a failed evaluation leaves recipe and document untouched), retire/`requireLive` gates, `Body.Placed` (re-evaluates the payload under the composed rigid motion), and `magnitudeIn` (Kind/finite/non-negative validation). |
| `extrude.go` | `Document.Extrude` (evaluator §5): gates via `RecordProfile`, `WithTaper` recorded-then-`ErrUnsupported`, extent resolution to the signed interval — `Distance`/`Symmetric`/`TwoSided` plus the body-relative stops (`ThroughAll`/`ThroughAllSide`/`ToFace`, resolved through `stops.go` with the stop bodies recorded as `StepRef`s in the step's `Inputs`) — and the analytic prism evaluator — shared-edge manifold topology, Exact volume/area/centroid, exact directional-extreme bounds (`prismPayload.extentAlong`, the arbitrary-direction extent the through-all stop reads). |
| `revolve.go` | `Document.Revolve` (evaluator §6): the sealed `Axis` vocabulary (`SketchLine`/`ConstructionAxis`/`EdgeAxis` + tagged codec; `Step.Axis` keyed to `OpRevolve` on the wire like `Angular`), the §6 axis gates (coplanar with the profile plane; region in one closed half-plane, either side; contact only as a segment endpoint or an on-axis `LineSeg` — interior tangency is `ErrDegenerate`), angular-extent resolution to the sweep interval (a 2π magnitude IS a full revolution, past it is `ErrDegenerate`; the body-relative `ToFaceAngular` stop resolves through `stops.go` AFTER the axis gates — it needs the resolved axis — with its stop body recorded in `Inputs` before the axis ref, deduplicated), `EdgeAxis` resolution (`resolveEdgeAxis`: the named body live in this document, the selector resolving to exactly ONE LINEAR edge — any other count is `ErrCardinality`, zero included, the core §12 implicit exactly-one; a non-linear edge is `ErrDegenerate`; the axis runs start→end vertex of the resolved edge, the step records the query with the body as its producing `StepRef` in `Axis` AND `Inputs`, and the body is depended on, never consumed), and the analytic revolve evaluator — `Cylinder`/`Cone`/planar-annulus/`Sphere`/`Torus` walls per segment kind, caps only on partial sweeps (a full sphere/torus face carries no loops), latitude-circle seam vertices, Pappus-exact volume/area/centroid, exact cylindrical-extreme bounds (`revolvePayload.extentAlong`). |
| `stops.go` | The body-relative stop resolution (evaluator §5/§6/§11 increment 2, core §8.1/§6.2). Shared gates mirror `EdgeAxis`'s: the named body live in THIS document (`resolveStopBody`: nil/wrong type `ErrDegenerate`, `StepRef` `ErrUnresolvedBody`, foreign/retired `ErrForeignBody`/`ErrRetiredBody`), the face selector under the implicit exactly-one (`selectStopFace`: any other count `ErrCardinality`, zero included; typed-nil query `errNilSelector`). `ToFace` (`resolveToFace`): the stop face must be PLANAR with its normal parallel to the sweep — the §5 build table's cap is planar and perpendicular — else `ErrUnsupported` (staged, never a wrong prism); a face coplanar with the sketch plane, a side-mismatched face on a `TwoSided` side, or an offset pulling the stop behind the plane is `ErrDegenerate`; standalone sense comes from the target face, sides from the side they occupy. `ThroughAll`/`ThroughAllSide` (`resolveThroughAll`): the stop is the farthest far side of every live body the sweep meets — "meets" judged EXACTLY on each body's closed-form `extentAlong` the sweep direction (material strictly beyond the sketch plane in the travel sense; the lateral footprint is deliberately not consulted — deciding it exactly is boolean-grade machinery, and a guess would fabricate or drop a recorded dependency); no body in the path is `ErrDegenerate`. `ToFaceAngular` (`resolveToFaceAngular` on the `angularStops` ctx): the stop face must be planar AND its plane contain the revolve axis (else `ErrUnsupported`), its material in ONE half-plane — located by exact boundary probes (vertices + arc midpoints/circle antipodes, `boundaryProbes`) with a closed-form arc axis-crossing audit — a both-sides face or one in the profile's own half-plane is `ErrDegenerate`; a standalone stop takes the nearer way around, a `TwoSidedAngle` side the side's own sense. Inputs assembly: named-extent refs in extent order, through-all stop bodies in stop order after them, the axis ref last, `dedupRefs` first-occurrence; `recordExtent`/`recordAngularExtent` substitute `StepRef`s + deep-copy selectors into the step. |
| `verify.go` | `Document.Verify` (evaluator §10/§11, verification design): `Report`/`BodyReport`/`Status` (worst-wins precedence) + `Trustworthy()`, `Interference`/`Clearance` row types, the `VerifyOption` set (every parameter validated at the call, never deferred into the report), the structural boundary audit (validity by construction), the Exact-passes tolerance gate, the pair partition — box-disjointness proofs first, then the always-on clearance-kernel proof (`clearance.go`) for every pair boxes cannot decide; an undecided pair reads `Suspect`, never a fabricated row — and the wiring of the increment-5 analytic surveys (`survey.go`): asked on a proven solid, each survey answers outright, the interval rule decides `MinWallThickness` against the tool (proven thin → `Violating` at any coarseness, met → the gate judges the bound, straddle → `Suspect`), a non-empty `Undercuts` is `Violating`, and `MinRadius` is a measurement, never a verdict. `WithClearances` emits a `Clearance` row per proven-disjoint pair — `Gap` = the kernel's interval midpoint, `Bound` its half width, `Exact` iff the interval is a point — gated by `gapWithinTolerance` (clearance §7: `Ref = max(\|Value\|, ε·D)` with `D` the PAIR's kernel-read diameter; a gap beyond tolerance makes the report `Suspect` directly); a gap the kernel cannot prove yields no row and reads `Suspect` only when asked. |
| `clearance.go` | The clearance pair kernel (docs/clearance-design.md §1/§2/§5/§6/§7; PR 1 of its §8 plan): `clearancePair` proves one pair's partition and gap interval. Order is load-bearing: the coplanar `Plane`×`Plane` contact certificate first (strictly opposing outward normals + coplanar carriers + proven positive-area trim overlap + each body's exact `extentAlong` wholly on its own side — a separating plane, so the whole contact set is certified and the gap is a measured `Exact` zero), then the tier enumeration; any `unsure` finding → undecided. The interval is `[min lo, min hi]` over contributions with §5 pruning (a `lo` at/above the best `hi` cannot hold the minimum); **exactness is the interval's, not the winner's** — `Exact` iff a closed-form winner has every rival's `lo` at or above it. `lo` must strictly clear the tolerance floor, then the §2 nesting exclusion runs (skipped when boxes already exclude it): one witness per shell (a vertex, else the loop-less face's synthesized point off stored data), certified ray casts against every face of the other body — a witness inside is a proven nesting (proven overlap → `Suspect`, per §7 no `Interference` row is minted without a boundable volume), an uncertifiable cast is undecided. Also the pair-`D` reading (vertices + face witness points; understating only lowers the floor). Proven-not-disjoint and undecided both return `pairUndecided` — PR 1 reports them identically. |
| `clearance_degen.go` | **The degeneracy oracle — the one gate between a closed form and a certificate.** Every cell that would emit a constant/`Exact` candidate, take the nested branch or grant a crossing exclusion asks here first, and the answer is THREE-valued (`degYes`/`degNo`/`degUnknown`, never a bool): `degYes` only on EXACT arithmetic over the payload's own floats (`ratV3` cross/dot products over `math/big.Rat` — clearance_poly.go's take-the-floats-exactly discipline), `degNo` only on a residual clearly above the kernel's noise, `degUnknown` in the band between. A tolerance may therefore route a question to `unsure`; it may NEVER route one to a certificate — deciding parallelism/coaxiality/on-axis-ness with `clrAngTol` mints an `Exact` the true answer undercuts by up to the tolerance. Members: `parallel`/`parallelSeg`/`parallelSegs`, `perpendicularSeg`, `onAxis`, `coincident`, `coaxial`, `ringFamily` (§4's coincident-spine families), and `spineSup` — the BOUNDEDNESS gate of §4's nested branch: `d_sup` is a SUPREMUM over the inner spine, so a cylinder's infinite LINE spine has `d_sup = ∞` against any bounded partner and is finite only against an exactly parallel line; a point spine's supremum is its own distance, a circle spine's needs exact coaxiality. `certifiedContainment` reads it, which is what keeps a rod piercing a ball from certifying as nested. The accepted cost (clearance §4): a rigid motion landing a meant-to-be-coaxial pair a few ulps off true reads `Suspect`. |
| `clearance_cells.go` | The §3 candidate sink + the §4 face-interior table. `cellSink.candidate` folds admission: rejected feet are discarded (a lower tier holds the minimum), a straddle keeps only its lower bound (never counted toward exactness), a near-zero value not cleanly rejected is `unsure` — a possible contact only §6 certificates may bless. The spine-offset reduction (`offsetPair`): spine-pair criticals (point/line/circle pairwise — CF where the oracle proves the configuration degenerate, else the P4/P8 brackets, else the coarse enclosure), the four offset combinations per critical, coincident-spine ring families for a PROVEN `ringFamily` only (`emitRingCombos` — the coaxial peg-in-hole reading; its azimuths are a deterministic SAMPLE, so an unadmitted near pairing keeps its proven lower bound rather than dropping a family that may hold the minimum), then crossing exclusion by the STRICT exterior branch, `certifiedContainment` (§4's list, through `spineSup`), or face-box separation; else `unsure`. The plane row: coplanar pairs reaching the cells are uncertified (`unsure` unless provenly apart in-plane), parallel plateaus admitted by projection overlap (`coplanarRelation`, probe-sufficient/exclusion-sound), crossing carriers excluded through trim-interval SUPERSETS on the shared intersection line (exclude-only, never bless), axis-parallel cylinder rulings/sphere and torus plane extremes CF with per-trim admission and exact crossing exclusion (`planeCrossesRevolved`'s axial range + the crossing circle of an EXACTLY perpendicular axis — a tilted axis crosses in an ellipse and earns no exclusion). `planeCone` emits no face-interior plateau: `|n·axis| = sin α` is a transcendental identity no exact test on the stored floats can certify, and the cell does not need it — the plane distance is affine along every ruling, so the minimum always migrates to the latitude edges and the synthesized apex, which hold it exactly. Cone-involved and spindle-torus cells are PR 2's BB path: coarse `[face-box distance, closest witness pair]` only. |
| `clearance_tiers.go` | The curve and vertex tiers of §3 over §4's curve-tier table: face×edge (`feCell`: line/circle × plane CF with in-plane contact handling, line/circle × offset faces via spine criticals ± the offset with `feCrossingExcluded`'s superset range; ×cone coarse), edge×edge (`eeCell`: line×line CF incl. the parallel family, line×circle P4 with the axis-parallel CF special case, circle×circle P8 with the coaxial CF special case), and the vertex tiers (`vertexTier`: point×surface and point×curve, all CF) — vertices include the synthesized cone apexes and spindle axis-collapse points (surface singular points where no interior first-order condition holds). Every constant-distance family here (a line parallel to a plane or to an axis, a circle whose plane parallels the face's, a coaxial cap edge, a parallel segment pair) is emitted only on the oracle's `degYes`: these objectives are affine along the carrier, so a `degNo` migrates the minimum to the trim boundary where the endpoint/vertex tiers hold it exactly, and a `degUnknown` owes an honest lower bound (`loOnly`, off the untrimmed carrier distance), a coarse enclosure or `unsure` — never a plateau. |
| `clearance_geom.go` | The kernel's boundary model (§2/§3): `bodyGeom` reads trimmed carrier faces off the evaluator's own payload (the same source the surveys read) — `cFace` with closed-form trims (angular×axial/meridian windows on revolution faces — a window is `full` only when it really closes on itself, since `full` is an ADMISSION and a tolerance rounding a window up to it admits carrier points in the sliver the trim is missing; `region2` line/arc-loop parity regions on planar ones, with strict/ambiguous/outside three-way `classify`, line-interval and conservative line-interval-SUPERSET queries, segment tests) — plus the topology's exact edges/vertices, per-face containing boxes and on-face witness points. Also the §2 nesting casts (`pointInBody`): a fixed deterministic direction ladder, crossings certified transversal and trim-interior per variant — plane/cylinder/cone/sphere closed form, the torus quartic by Sturm isolation (`torusCrossings`: a non-square-free quartic is a tangency, ambiguous); a carrier through the ray start counts only if the start provably misses the trim (witnesses on shared construction planes). |
| `clearance_poly.go` | The certified-bracket machinery of §4/§5: `ratPoly` over `math/big.Rat` (Sturm chains, square-free reduction, Cauchy-bounded root isolation, deterministic bisection refinement with a fixed depth budget — on exhaustion the honest wide interval stands), the `csPoly` trig-polynomial form `A(c)+s·B(c)` with exact d/dθ, quarter-turn chart shift and half-angle conversion (two charts cover the circle), and the cell builders — `lineCircleBrackets` (P4) and `circleCircleBrackets` (P8, the squared stationarity `s·(w′)² = r²·(s′)²`, ρ=0-kink guarded by callers) — each returning enclosures of every critical VALUE via a proven Lipschitz bound (distance to a fixed set is 1-Lipschitz, parameterization speed ≤ r). Coefficients are the evaluator's own floats taken exactly: a root count is a proof only when the isolation certifies it. |
| `survey.go` | The increment-5 analytic surveys (verification §6, evaluator §10): wall/undercut/min-radius answered outright on the evaluator's own payloads. The wall reading reduces exactly to 2D — a prism's spanning balls are its profile's spanning disks with the height as the vertical fit (the parallel caps span when a disk of half the height fits the section), a revolve's are its meridian section's (mirrored across the axis for a full turn, since a ball's contacts with surfaces of revolution lie in the meridian plane through its center; wedge-constrained by the caps for a partial one, whose on-axis cap edge has the sweep angle itself as its dihedral); a junction dihedral within the allowance pinches to a genuine `Exact` 0 (knife edges, in-allowance tapers). Undercuts are per-face normal-range membership over exact per-variant ranges: a face with a provenly opposing point is listed, exactly perpendicular clears — **and so does a face exactly antiparallel everywhere** (a prism's flat base separates under the pull rather than hooking it; the one reading that makes the proven empty all-clear §1 promises attainable on a closed solid — a deliberate, documented narrowing of §6's pointwise `n·p < 0`); walks map to `*Face`s by feature role. `MinRadius` is the tightest concave principal radius (meridian arcs walked against the material, plus the parallel circle's ρ/\|n_ρ\| where the meridian normal points at the axis — hole walls, a donut's waist). `nil` readings are proven absences; a payload the surveys cannot decide leaves the asked question undecided → `Suspect`, never a silent pass. |
| `survey2d.go` | The 2D kernel behind the wall survey: a closed-form candidate set of critical inscribed disks — Apollonius triples (material-side-pinned tangency equations; quadratic pairs linearize by subtraction, parallel linear pairs pin the radius), antipodal pair criticals, angle-limit disks at exactly the allowance boundary (the closed-under-limits families of §6; within is inclusive, so a wall drafted at exactly α spans), concentric whole-arc contacts, and wedge-tangent minima — each validated against the whole boundary: distance clearance, ray-parity containment (ambiguous hits retry across 16 directions), and contact-direction spanning over `dirArc` sets. The set is complete for the attained infimum/supremum over line/arc boundaries, so the minimum spanning diameter and the 2D inradius are exact; a containment the retries cannot decide fails the survey to `Suspect`. |
| `tessellate.go` | `Mesh` + `Body.Tessellate(tol)` (core §11, evaluator §9): an OUTPUT, never the representation. Chordings are chosen once per boundary curve and shared by every face meeting it, so the mesh is watertight and consistently oriented by construction; facets remember their source `*Face`; `Mesh.Bound` is the proven max chord sagitta (≤ tol; zero for an all-planar body). Tolerance validated like every magnitude, zero → `ErrDegenerate`; a body this evaluator did not build → `ErrUnsupported`. A boolean-built body restates its held polygons at their own bound (finer is `ErrUnsupported` — the analytic identity is gone), and every mesh carries `areaSlack`, the closed-form chord-versus-arc area deficit the boolean's area bounds compose from. |
| `triangulate.go` | The cap triangulator behind `Tessellate`: polygon-with-holes → CCW triangles by hole bridging (Eberly max-u visibility) + reflex-blocked ear clipping — correct for non-convex outlines with holes, deterministic; a stalled clip is `ErrDegenerate`, never a wrong mesh. |
| `export.go` | `Body.STL`/`Body.OBJ` (core §11): deterministic writers over `Tessellate` — ASCII STL (chosen for diffable, byte-stable output), OBJ vertex table + 1-based faces. `WithChordTolerance` is an `STLOBJOption` (jwx combined-interface pattern) both accept; without it the tolerance defaults to 1/1000 of the body's bounding-box diagonal. |
| `bounds.go` | The single owner of every proven error bound a faceted (boolean) measurement reports — ONE helper per error mechanism, and NO measurement site computes a bound inline. `radius3D` (a per-coordinate bound read as a 3D distance is ×√3, up-rounded); `sumSlop` (a PROVEN bound for the NAIVE float sum that produced the value — never zero for a nonzero float-computed quantity, which is what keeps `exactnessOf` from claiming an Exact a float sum of square roots does not have); `chainLengthBound` (a rim of nSegs chords, BOTH endpoints of each displaced: 2·nSegs·δ + the sum's own slop); `rigidRoundAllow` (ulp at the INPUT and translation magnitudes, ×√3); `sweptVolumeAllow` (the volume a vertex displacement moves — δ × the area of the surface the displacement acted ON, which is NEVER the area of the mesh that survived it) over `perturbedAreaUpper` (the area of a mesh whose vertices may each sit δ from the held ones, and of every mesh on the way — the only term that speaks for a facet the weld flattened to zero area); and `rimDelta` — the trim amplification: a vertex the boolean CREATES is the crossing of two chord PLANES, so it is displaced by (δA+δB)/sin θ, unbounded as the operands approach tangency, and when the inflated bound reaches the pair's diameter it has stopped bounding anything and the operation is `ErrUnsupported`. |
| `boolean.go` | `Union`/`Cut`/`Intersect` (core §8, evaluator §9): package-level, no target-out, operands retired and the result registered through the operands' own document; gates (nil/self `ErrDegenerate`, `ErrForeignBody`, `ErrRetiredBody`); the evaluator-internal chord tolerance (pair bounds-box diagonal × 2e-5 — a documented default, never caller-set; its whole effect surfaces as the result's proven `Bound`); the TANGENCY GATE (`refuseUndecidableProximity`) — a chord polygon lies strictly inside its curved surface, so a true tangency can vanish from the tessellation and the verdict would be decided by where the chords fell; per analytic FACE pair, a pair whose facets MEET is decided (the contact is exact), a pair that comes within δA+δB WITHOUT meeting is `ErrUnsupported` (reject-only; a planar face with straight edges and a `Faceted` face are held exactly, so they have nothing to prove); and the bound composition — the volume error by the symmetric-difference identity (Σ per-operand δ·held-area + the final rounding's swept volume, charged against the PRE-ROUND stitched surface so the facets the weld collapsed still answer for what they carried; sound for all three ops, the rim never entering it), the BOUNDARY by `rimDelta` (the trim-amplified (δA+δB)/sin θ, not δ), centroid by displaced mass against the pair diameter. The step records `Inputs` as the two operands' `StepRef`s — `[target, tool]` for cut — and consumes both. An empty result (disjoint intersect, all-removing cut) is `ErrBooleanFailed`; the recipe and document stay untouched on any failure. |
| `boolean_mesh.go` | The §9 mesh-boolean pipeline. ONE symmetric classifier owns every contact question (`triTriClassify`): two closed triangles are convex, so their intersection is empty / a point / a segment / a 2-D region, and the routine computes WHICH — direction-free, with endpoint-on-boundary flags decided by testing the point against EACH triangle (`onTriBoundary`), never by which crossing list it came from. A 2-D region is a coplanar face-on-face tangency (`ErrDegenerate`); an isolated point contact no crossing chain owns is `ErrDegenerate`; branching intersection curves are `ErrDegenerate`. GRAZE-VS-CROSSING LEAVES THE PAIR: a segment running ALONG a facet edge is only REPORTED by the pair (`segAlongEdge`); `meshBoolean` decides it once with the adjacency in hand (`edgeCrosses`) — the edge's two adjacent facets strictly on one side of the other facet's plane is a true graze (`ErrDegenerate`), straddling is an ordinary crossing whose segment subdivides only the facet it passes THROUGH (a segment on a facet's own boundary cuts nothing off it) and whose regions classify by exact parity (`viaParity` — the other operand's boundary there is a DIHEDRAL, and one plane of a dihedral decides nothing), with the crossed mesh edge BLOCKED so the uncut flood fill cannot merge the two facets it separates. Classification otherwise: a chain-anchored region by the exact side of its partner facet's plane; an uncut component by one exact axis-ray parity seed, flood-filled. The pass also returns the smallest crossing-angle sine it saw (`sinSquared`/`sinLowerBound`), exactly from the facet normals — what `rimDelta` divides by. Then stitching by shared exact vertices, the conforming pass (a vertex exactly interior to another facet's edge splits that facet — exact incidence, nothing moves), the directed-edge closure audit (`ErrBooleanFailed` on any break), then float rounding with coincident-float welding, re-audited. A COLLAPSED OPERAND facet (a rigid placement's rounding can flatten one) is refused at `prepBoolMesh` (`ErrUnsupported`): with no plane and no interior it is invisible to every contact predicate here, so a contact made on it would be classified by nothing at all. What the WELD collapses is answered in two ways: a facet dropped from a component that SURVIVES is an edge contraction, bounded (its swept volume charged against the PRE-ROUND surface, `stitchedMesh.preArea`, and the area it takes with it, `dropArea` — the held mesh's own area is the wrong yardstick, the dropped facet is missing from it); a component EVERY facet of which collapses is welded out of existence and is refused (`refuseWeldedAwayComponent`, `ErrUnsupported`) — the lost lump is a topological loss no bound can speak for, and the closure audit misses it because the components that remain still close. |
| `boolean_cut.go` | Per-facet exact subdivision: contact segments interned into chains (endpoints on the facet's own boundary; a closed chain — the other solid punching through the interior — is opened by vertical split lines chosen through no existing vertex), convex line splits, polygon splits along chain paths, exact ear clipping that keeps every collinear vertex (what keeps a conforming subdivision conforming), and the per-region classification anchor. All rational 2D on the facet's own plane projection. |
| `boolean_exact.go` | The exact-arithmetic kernel (§9's discipline): adaptive orient3d — a float evaluation with a proven forward-error filter, falling back to `math/big.Rat` exactly at the boundary cases — plus the rational point/segment/polygon predicates and the axis-ray mesh parity test with deterministic retries (an ambiguous ray tries the next axis; all six ambiguous is a loud failure, never a guess). |
| `boolean_body.go` | `facetedPayload` + `buildFacetedBody`: facets grouped by (component, source face) so `FaceCreatedBy` provenance survives the boolean, real Loop/Edge/Vertex topology chained along face boundaries (chains split where the face pair or the exact hinge convexity changes; loops walked through the facet fans; a face's outer loop is its longest — bookkeeping, validity never reads it), shells/voids by exact signed volume + exact containment parity, and measurements integrated exactly (`big.Rat`) over the held float mesh, reported with the composed proven bounds — every one of them from `bounds.go`, NEVER computed inline. `exactnessOf`'s zero bound is a CLAIM that the value is exactly representable, so only the exact rational integrals (carrying their own explicit rounding term) can reach it: an all-planar boolean whose contacts round exactly keeps an honest Exact VOLUME, while a float-computed length or area never does. The area bounds multiply the FACE perimeter — the sum of the faces' own loop lengths, which counts every rim twice, once per face it separates and perturbs. `placed` re-evaluates under the delta motion, charging the transform's rounding at the INPUT and translation magnitudes (`rigidRoundAllow`) — where it is actually committed, not at the result's. |
| `examples/` | Executable Go examples (`Example_decad_…`, `go test`-verified `// Output:` blocks) that double as living documentation. Never `package main`. |
| `.github/workflows/` | `ci.yml` (lint → test/tidy/govulncheck), `codeql.yml`. |

## Conventions

- Go style, testing and file-layout rules: `~/.claude/docs/go.md`. Tests use
  `testify/require` (never `assert`), external `_test` package, `t.Context()`.
- User-facing usage → executable Go examples in `examples/` with verified
  `// Output:` blocks. NEVER README-only snippets.
- Docs state **current state only** — no changelogs, no "was X, now Y".
- Design docs live in `docs/<topic>-design.md`.

## Verification

```
go test ./...      # must pass
go vet ./...       # must pass
golangci-lint run  # v2.12.2, config in .golangci.yml
```
