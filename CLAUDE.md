# CLAUDE.md

Guidance for working in this repository. Read before making structural changes.
Update when a design variable gets resolved.

## What this is

A **headless CAD engine** in Go — the 3D modeling layer above the `sketch` 2D
constraint engine. Build solids in code, then interrogate them programmatically.

**North-star use case:** a *headless 3D verification oracle*. A coding agent
models a part here and proves it sound — watertight, correct volume, no
interference, no wall thinner than the tool — BEFORE committing to write real
CAD software code (e.g. an Autodesk Fusion add-in). Be wrong in the cheap place.

**Current state: the public API is landing incrementally against an approved
design.** What the package exports today is the leading edge of that surface;
everything it does not yet export remains design-only, and every capability the
design consumes exists in its dependencies — there is no open dependency gap.
`docs/api-design.md` is the core contract for the whole surface: a
recipe/evaluator split, a B-rep-shaped surface, immediate-mode features,
selectors instead of handles, and `Exactness` on every measurement. Companion
designs carry its deep ends: `docs/sketch-seam-design.md` (the trim
contract and recording IR at the `sketch` seam),
`docs/recipe-replay-design.md` (strict recipe decoding, validation, versioning,
resource limits, and atomic evaluation), `docs/verification-design.md` (the
tolerance gate and noise floor), `docs/tessellation-design.md` (the mesh
contract, per-payload chording, proof bounds, and boolean handoff),
`docs/payload-verification-design.md` (payload-specific proofs),
`docs/interference-design.md` (read-only overlap proof and bounded volume),
`docs/spline-design.md` (the free-form kinds' exactness tiers and reach), and
`docs/prism-boolean-design.md` (the analytic reduction for `Union`/`Cut`/
`Intersect` over co-directional coplanar prisms, routed through `sketch`).
Read them before writing any public type. Read the replay design before
changing recipe codecs or evaluation entry points. The Layout table lists
every current design document. `docs/evaluator-design.md` is the v1
evaluator's own design — topology construction, mass properties, staging
(`ErrUnsupported`), the mesh boolean, the Verify implementation; read it
before writing evaluator/topology/feature code. Read the tessellation design
before writing tessellation, export, or mesh-boolean operand code. Read the
spline design before writing any free-form geometry, `NURBSSurface`/
`NURBSCurve` code, or per-segment-kind dispatch. Read the prism boolean design
before touching `evaluateBoolean`'s dispatch, `Union`/`Cut`/`Intersect`, or any
code that combines two of decad's own recorded sections through a private
`sketch` scene.

## Hard rules

- **Layering is `decad -> sketch -> r3 -> units`.** decad imports all three
  directly. NEVER import decad from any of them; they do not know it exists.
- **NEVER re-derive a 2D answer.** Profile closure, DOF, constraint conflicts,
  sketch validity, an intersection, a cut parameter, a projection onto a curve →
  ask `sketch`, consume its answer. Where `sketch` reports its own answer
  approximate — a `Partial` fragment whose cut is sampled, or an uncertified
  `Partial` fragment (`BoundaryEdge.TExact` false;
  `docs/sketch-seam-design.md`) — decad **rejects**. It never repairs,
  projects, fits, or infers the exact answer. A whole (non-`Partial`) edge
  records from the entity's own data and never consults `TExact`. Building a
  private `sketch` scene from decad's OWN recorded entities and asking it to
  arrange them is not re-deriving an answer — the moments engine already does
  this for authentication (`moments_validate.go`), and
  `docs/prism-boolean-design.md` extends it to combining two recorded sections:
  decad selects among the regions `sketch` returns; it never computes the
  crossing, cut parameter, or containment itself.
- **A decad-side check may only FALSIFY an upstream claim, never bless one.**
  Admission is decided by what `sketch` says — `BoundaryEdge.TExact` for a
  `Partial` fragment — never by a test decad runs on the
  geometry it was handed. A residual against a source curve is admissible in exactly
  one direction: **large ⇒ the claim is disproven ⇒ reject**; **small ⇒ proves
  nothing** — a sampled cut can lie arbitrarily close to the curve, so a small
  residual NEVER admits an input (`docs/sketch-seam-design.md`). A check that can accept
  is an admission gate, and an admission gate on a residual is unsound. Reject-only,
  always.
- **NEVER hand-roll coordinate math.** Vectors, frames, local↔world transforms →
  `r3`. Its `Frame` is orthonormal, so the inverse is the transpose, never a
  matrix solve.
- **Shapes belong HERE.** `r3` excludes them by charter; solids/surfaces/meshes/
  topology are this module's job.
- **NEVER add a public API that contradicts the design docs** —
  `docs/api-design.md` and every design document listed in the Layout table.
  Extending them is fine; changing a decision means changing the doc first.
- **NEVER expose triangles as the representation, indices as selectors, or a bare
  `float64` measurement. NEVER give a boolean a target-out parameter or let it
  mutate an operand.** These are the forward-compatibility invariants that keep
  an exact-kernel future reachable (`docs/api-design.md` §3). Scalar quantities —
  values and their error bounds alike — are `units.Value`. Exactly two things are
  not scalar quantities and so fall outside the rule (`docs/api-design.md` §5.2):
  the **coordinate** — an `r3.Vec`, or a plane-local `Point2` — which is a length in
  millimetres by convention; and the **curve parameter** — a spline's degree, knots
  and weights, a recorded segment's parameter range (`TStart`/`TEnd`), a conic's
  fullness `Rho` — which is a dimensionless index into a parameterisation, not a
  measurement of anything.
  Neither is a licence for a bare float anywhere else.
- **NEVER add a `go.mod` module without recording the decision here.** Approved:
  - `github.com/lestrrat-3d/sketch` — parametric 2D constraint engine.
  - `github.com/lestrrat-3d/r3` — 3D coordinate math (`Vec`, `Frame`,
    `Transform`).
  - `github.com/lestrrat-3d/units` — typed quantities (`Value`, `Kind`).
    Direct: decad's `Measurement` and `Recipe` quantities are `units.Value`.
    It is the same module `sketch` uses for its dimensions (`sketch` has no
    in-tree units package), so there is no parallel unit system to reconcile.
  - `github.com/lestrrat-go/option/v3` — functional options (house library). Used
    by feature options.
  - `github.com/stretchr/testify/require` — assertions, **test code only**.
    NEVER import from production code.
- **Correctness must be observable.** Every capability ships with a test
  asserting on computed geometry (coordinates, volumes, residuals) — NEVER
  merely "it ran".

## Layout

| Path | Responsibility |
|---|---|
| `docs/api-design.md` | **The core public API contract.** Recipe/evaluator split, forward-compat invariants, feature + selector + verification surface. Points at the companion designs. |
| `docs/sketch-seam-design.md` | The recording contract at the `sketch` seam: trim contract (`TExact`), the `CurveSegment` recording IR, `ErrUnrecordableProfile`. |
| `docs/recipe-replay-design.md` | Stored-recipe contract: bounded canonical encoding, strict/versioned JSON, validation + liveness graph, deterministic error precedence, resource limits, package-owned evaluator boundary, shared recorded-step dispatch, whole-recipe atomicity, replay tests. |
| `docs/verification-design.md` | How `Verify` judges every bounded result: report + statuses, default interference cost and caller deadlines, `WithTolerance`, the diameter-anchored noise floor. |
| `docs/payload-verification-design.md` | How `Verify` covers every evaluator payload: `loftPayload` construction validity and staged pair/survey handling, the exact cup boundary and wall proofs, faceted boundary certificates, bounded validity/clearance/survey algorithms, implementation order, and required tests. |
| `docs/evaluator-design.md` | The v1 evaluator: evaluate-from-the-record rule, topology model + provenance roles, mass properties on decad's own records, per-feature build tables, explicit staging via `ErrUnsupported`, the exact-predicate mesh boolean, Verify's implementation, the increment plan. |
| `docs/tessellation-design.md` | The single tessellation contract for prism/cup/loft/faceted/revolve payloads: `loftPayload` exact restatement, shared curve samples, global revolve angular samples, pole/apex + vertex-link manifold proofs, source faces, two-sided bounds, weld-inclusive boundary certificates, area slack, symmetric-difference proofs, boolean admission, refusals, and increments. |
| `docs/clearance-design.md` | The increment-3 clearance kernel: how a minimum gap between two analytic bodies is proven — the stationarity-tier candidate enumeration with exact trim admission, the face-pair distance table (closed-form / certified polynomial bracket / branch-and-bound), the disjointness proof (boundary clearance + nesting exclusion), touching-contact classification, and how rows feed the report. |
| `docs/interference-design.md` | The normative design for non-mutating pairwise overlap proof: four-way pair relation, strict full-containment volume reuse, read-only mesh intersection and its facet-pair cost, positive-volume gate, shared boolean bounds, stable order, cancellation, refusal rules, and staged coplanar/revolve breadth. |
| `docs/modify-design.md` | The increment-5 modify ops (`Fillet`/`Chamfer`/`Shell`), stated in four normative tables — the receiver (keyed on PAYLOAD CLASS, so a tube is a `prismPayload` and builds), the refusals (every one once, its sentinel picked by the existence test: no such body → `ErrDegenerate`; a body this evaluator cannot build → `ErrUnsupported`), the result (op × removed faces × sense × section connectivity → payload, LUMP COUNT, faces + roles; a both-caps shell of a holed section is `1 + k` lumps and refused), and the downstream consumers (payload-only readers vs role-keyed ones). Plus: the reduction that keeps their analytic shape and propagates bounded mass results, with Exactness only when the proven bound is zero — a straight prism's modify ops are rewrites of its recorded section — the blend/bevel construction and its refuse-never-clip cutback gates, the exact section offset, the build-time simplicity + nesting audit, the cup payload, and the role rule (a role indexes the record it labels, so a result's roles are minted from the result's own record — never inherited). |
| `docs/spline-design.md` | The free-form kinds' design: §2's whole-entities-only scope and the two record-level exclusions behind it (§2.1's trimmed fragment, §2.2's elliptical arc); Table F's per-kind exactness tiers and §3's Tier A rounding rule; Table R's refusals and their sentinels, with §4.1's derivation of the modify ones and §4.1's analytic-corner slice; §5's exact rational Tier A moments, their work budget and the Tier B/C constructions, with §5.1.1's knot-above-the-degree rule; §6's proven brackets for the readings no tier makes exact — arc length, §6.1.1's radial first moment `∫ r ds`, directional extremes, chord sagitta, normal cones and curvature, all reduced to `clearance_poly.go`'s certified root engine — under §6.3's speed-floor and origin-exclusion certificates and §6.4's build-time bracket gate; §7's opaque `NURBSSurface`/`NURBSCurve` variants; Table C's per-capability reach and §8.1's wall-thickness kernel; §9's upstream asks to `sketch`; §10's increments; §11's test obligations. |
| `docs/modify-reach-design.md` | The approved modify extension: exact tangent-chain expansion, reference-face asymmetric chamfers, revolve junction rewrites, complete prism cap-loop blends, stacked-prism side/no-opening shells, allowed revolve shells, permanent faceted-receiver refusal, exact proof gates, payload topology/roles/measurements, downstream staging, and required tests. Read with `docs/modify-design.md` before extending any modify op, option codec, or modify payload. |
| `docs/loft-design.md` | The first count-free `Loft` PR design, un-deferring it from api-design §8/§13: two profiles on distinct geometric planes ruled between corresponding points, stated in four normative tables — pairing (Table P: ordinal correspondence fixed from recorded segment order alone, never sampled/fitted/nearest-point, with an explicit per-loop rotation offset as the only caller-stated intent), the refusals (Table S, same existence-test sentinel discipline as `docs/modify-design.md`), the result (Table B: every wall a flat triangle pair split along the prism's own bottom-start/top-end diagonal convention, so every face is an exact `Plane`), and the downstream consumers (Table D, staged the same way the cup's clearance/survey rows are). The first PR admits only `LineSeg`-to-`LineSeg` correspondence; same-kind `CircleSeg`/`ArcSeg` correspondence and N-section and guide-rail/centerline lofts are deferred to PR 3, while free-form, mixed-kind, and reversed correspondence are permanently excluded. Volume uses a single-rounding exact-rational tetrahedron-sum accumulator. Centroid rounds its three exact rational coordinates once each into a `VecMeasurement`, whose bound is their rounding-error radius. Area is never exact, for the same reason arc length never is. The build-time wall-crossing audit reuses `boolean_exact.go`'s adaptive orient3d, `boolean_mesh.go`'s `triTriClassify`, and §6's `triTriCoplanarSharedEdge`; it never adds a bracket engine. |
| `docs/prism-boolean-design.md` | The analytic reduction for `Union`/`Cut`/`Intersect` over co-directional coplanar prisms, dispatched inside the existing booleans behind a reject-only entry gate (§3: payload class, exact plane coplanarity/co-directionality via `xform ∘ frame`, line/circle/arc-only segments, the per-op z-interval relation) — a miss falls back to the unchanged mesh path with no error. An admitted pair combines both operands' `ProfileRecord`s through a private `sketch` scene decad builds and arranges (§4: `Union`'s hole-free select-all/merge, `Cut`/`Intersect`'s clean-nesting structural whole-loop match, and the staged general per-cell edge-orientation classification), authenticates every surviving segment through the existing `recordEdge`/`falsifyRange` seam (§5), audits a merged assembly with the unmodified modify §5 crossing/orientation/nesting checks (§6), and rebuilds through `evalPrism`, which §7 extends with one section-displacement bound so the re-expressed operand's own rounding reaches every measurement — a first-class `prismPayload` that chains, fillets/chamfers/shells, surveys, and enters the clearance kernel for free (§8), with the recipe wire format unchanged (§12). Read before touching `evaluateBoolean`'s dispatch or `Union`/`Cut`/`Intersect`. |
| `doc.go` | Package doc: scope + the layering contract. |
| `errors.go` | The core §12 sentinel error vocabulary — one branchable identity per case an agent must branch on — plus path-aware `RecipeError` (replay §6) and the H2 typed `BooleanError` (core §8): `Op`/`Inputs`/`Code` with the `BooleanErrorCode` set (`BooleanEmpty`/`BooleanEvaluatorFailure` wrap `ErrBooleanFailed`, `BooleanUnsupportedContact` wraps `ErrUnsupported`), so `errors.Is` keeps branching on the sentinel while `errors.As` reads the fine result. |
| `measurement.go` | The bounded-result shapes (core §5.3/§6): `Exactness`, `Measurement`, `VecMeasurement`, `Box`. |
| `record.go` | The recording IR (seam §2): `PlaneRecord`, `Point2`, `ProfileRecord`, `LoopRecord`, the ten sealed `CurveSegment` variants, and their tagged JSON codec (pointer variants normalize to values; nil pointers and unknown/missing kind tags are rejected). Segment validation splits along the same seam the moment preflight needs: `validateNURBSSegmentSizes` is the O(1) structural half (degree, control/knot/weight COUNTS) and reads no element, `validateNURBSSegmentContent` is every per-element check — each per-element diagnostic formatted only for the element that FAILS, so the scan costs the slice walk and not a description per knot and per weight (spline design §5.2) — and `validateNURBSSegment` is the two in that order. `validateNURBSInteriorMultiplicity` refuses an interior knot only where the recorded curve BREAKS APART (spline design Table R row R12): at multiplicity `degree+1` or more the two one-sided limits are the recorded control points `P_j` and `P_{j+m−degree}`, so continuity is decided by exact identity of those two coordinates alone — different is `ErrDegenerate`, identical is a continuous curve this record ADMITS and the evaluator's own slicer refuses as R13. |
| `seam.go` | The seam conversion: `RecordProfile(s, p)` — applies the §7 source/staleness/validity gates (`ErrForeignProfile`/`ErrStaleProfile`/`ErrInvalidProfile`), authenticates every non-nil boundary entity against `s.Entities()`, exact-matches every exported field to one fresh `s.Profiles()` result and records that trusted snapshot; then applies the `TExact` admission gate and reject-only falsifier (`falsifyRange`, evaluating through `sketch/geom`'s own evaluators — the only `Polyline` read, endpoints only, check-never-record). |
| `extent.go` | The extent vocabulary (core §8.1): `Direction` (named text codec), the sealed linear `Extent`/`SideExtent` tiers (`Distance`/`ThroughAll`/`Symmetric`/`TwoSided`; `DistanceSide`/`ThroughAllSide`) and the sealed angular `AngularExtent`/`SideAngular` tiers (`AngleExtent`/`FullRevolution`/`SymmetricAngle`/`TwoSidedAngle`; `AngleSide`), each with its own tagged codec — the two sets are disjoint in the type system and on the wire. The body-relative stops `ToFace` (both `Extent` AND `SideExtent`; `Offset` the one SIGNED number in the vocabulary — positive overshoots the face, the zero Value means none) and `ToFaceAngular` (both `AngularExtent` AND `SideAngular`, no offset) are core §8.1's deliberate dual-tier exceptions: their codecs record the body as its producing `StepRef` (a live `*Body` never encodes, like `EdgeAxis`), decode through pointer wire fields, reject nil variant pointers (wrap `ErrDegenerate`), and the clone helpers (`cloneExtent`/`cloneAngularExtent`) deep-copy their selectors so no caller-owned query survives into a recorded step or escapes `Recipe()`. |
| `recipe.go` / `recipe_wire.go` | The Recipe IR and strict/versioned root codec (`docs/recipe-replay-design.md` §2): `Recipe`/`Step`/`StepRef`/`OpKind` (named text codec, the copy ops `OpDuplicate`=`"duplicate"`/`OpPlacedCopy`=`"placed_copy"` beside `"placed"`)/`BodyRef`/`Selector` root/`StepOpts` (`ExtrudeOpts`/`ShellOpts`/`LoftOpts`), and Step's wire codec — absent fields omitted, every sealed field (selectors included) dispatched through its closed-set codec; required option payloads (`taper`/`sense`/`profile2`/`plane2`) decode through presence-aware pointer wire fields; `LoftOpts.alignment` is optional and its absence records zero offset for every loop; extent/angular one-of keying and presence-aware bidirectional `Placement` keying (`validatePlacementKeying`: `OpPlaced`/`OpPlacedCopy` REQUIRE a present, nonzero-valid `Placement` — `r3.Identity()` among them; every other op including `OpDuplicate` FORBIDS the field, a present zero-value one rejected — presence read from the wire field on decode (a `json.RawMessage`, so an explicit `null` and a `{}` both count as present, never mistaken for absent), `nonzeroPlacement` on marshal) are enforced on both wire directions. |
| `selector.go` | The selector vocabulary (core §9): `EdgeSelector`/`FaceSelector`, the `*EdgeQuery`/`*FaceQuery` variants of `Selector` (predicate conjunction + `Exactly`/`AtLeast` cardinality), the `EdgePredicate`/`FacePredicate` constructors (`Convex`/`Concave`/`ParallelTo`/`LongerThan`/`CreatedBy`/`Circular`; `Planar`/`Cylindrical`/`NormalTo`/`Facing`/`FaceCreatedBy`), the typed cap-role helpers `CapStart`/`CapEnd` (each `FeatureRef{Step: b.Origin().Step, Role: "capStart"/"capEnd"}`, so `FaceCreatedBy(CapStart(b))` needs no string literal; well-formed on any body, matching nothing where the step mints no cap), and their tagged codec. The variants seal in with POINTER receivers, so there is no value form to normalize to: nil pointers are rejected (`errNilSelector` wraps `ErrDegenerate`) and the clone helpers deep-copy — no caller-owned pointer survives into a recorded step, none escapes `Recipe()`. Resolution (`SelectEdges`/`SelectFaces`, evaluator §7) is the filter pipeline over the body's live topology: gather (`Body.Edges()`/`Faces()`, whose order the result keeps — deterministic, so a recipe replay selects identically), validate predicate parameters up front (zero direction `ErrDegenerate`, non-finite `ErrNotFinite`, `LongerThan` non-length/negative `ErrUnitKind`/`ErrNegativeMagnitude`), filter by conjunction, then enforce cardinality (`Exactly`/`AtLeast` miss is `ErrCardinality` even at zero matches; unasserted zero is `ErrNoMatch` — core §12 precedence). A failing resolution wraps that sentinel in a `SelectionError` (`selection_error.go`) carrying the query rendering, body, expected/actual counts and per-clause residuals; malformed-count assertions stay bare `ErrDegenerate`. Matching is decided on what an entity IS: `ParallelTo` compares a LINEAR edge's start→end direction (either sense; a curved edge never matches), `NormalTo` a PLANAR face's plane normal (either sense), `Facing` the same but ONE sense — the face's OUTWARD (material-leaving) normal points ALONG v (parallel AND positive dot, the `Face.reversed` sign), so it picks one of a slab's two caps where `NormalTo` picks both, `LongerThan` is strict on `Edge.Length()`, `Circular` means `Circle3`/`Arc3`, `Convex`/`Concave` read the decided `IsConvex` (the walked-boundary convention — `Concave` is what picks a hole's rim), `FaceCreatedBy` matches ANY of the face's multi-role `Origins()`, and `CreatedBy` matches an edge through its adjacent faces' `Origins()` (an edge carries no roles of its own). Resolution reads only the public topology, never the payload — prism and revolve bodies alike. |
| `selection_error.go` | The selector-failure diagnostic surface (core §9): `SelectionError` (wraps `ErrNoMatch`/`ErrCardinality`, `Unwrap` returns the sentinel so `errors.Is` branches) with `Kind`/`Query`/`Body`/`Expected`/`Actual`/`Residuals`, the `SelectorKind` (`EdgeSelectorKind`/`FaceSelectorKind`, singular `edge`/`face` `String()`) and `PredicateResidual`. `(*EdgeQuery).String()`/`(*FaceQuery).String()` render canonically — `<kind>(<pred>, …)<cardinality>` from the codec's own tokens, total (a kind-less clause renders `<invalid>`, a nil query `edges()`/`faces()`), with the load-bearing normalizations: a coordinate vector is `x,y,z` with `-0.0` normalized to `0` before shortest-round-trip `FormatFloat` (so value-equal queries render identically), a `LongerThan` value is `units.Value`'s own text (`10 mm`), a `FeatureRef` is `<step>:<quoted role>`. `residuals` recomputes the cumulative per-clause match counts only on the failing path. `impliedOneFace`/`impliedOneEdge` build the implicit exactly-one `SelectionError` (`Expected "exactly 1"`) a stop or axis reports; they take the concrete `*FaceQuery`/`*EdgeQuery` (the implicit-one callers gate the selector to the built-in variant first), so no accepted path can miss a `SelectionError`. |
| `moments.go` / `moments_validate.go` | The mass-property engine (evaluator §4): validates supported fields, delegates mixed line/arc region topology to a private `sketch` reconstruction, directly validates whole-circle containment, then applies closed-form Green's-theorem boundary integrals — `Area()` (a `Measurement`, Kind Area, Exact only when the computed bound is zero), `Centroid()` (a `VecMeasurement` whose Value is PLANE-LOCAL (u, v, 0) mm) and `SecondMoments()` (∫u²dA/∫uv dA/∫v²dA about the plane origin, Kind SecondMomentOfArea — what a revolve centroid needs) each carry their own proven bounds — closed forms for line/circle/arc walks incl. pointer variants. Every exact contribution — line and Tier A free-form alike — is also summed into ONE region-level `exactMoments` rational accumulator, re-referenced to the profile origin over rationals and rounded to float ONCE at the end (`addExact`/`translateExactMoments`/`publishExact`), so a MULTI-segment region reports the single-rounding bound spline design §3 requires and not a sum of per-segment roundings; a circular contribution has no exact rational, retires the accumulator (`dropExact`) and leaves the per-segment float accumulation with its own outward bounds standing. The WALK ANCHOR is subtracted along two arithmetics on purpose (`regionIntegrals.add`): the float evaluation reads float-shifted coordinates, which is what keeps it conditioned, while EVERY exact rational — a line's closed form, an arc's proven area interval, a converted free-form chain — subtracts the anchor over rationals from the RECORDED coordinates, because `fl(p − anchor)` rounds and an exact result taken over rounded coordinates is the exact answer for a region nobody recorded, published as `Exact` with a zero bound. Tier A free-form kinds (`docs/spline-design.md` Table F) route through `spline_bezier.go`/`spline_moments.go` rather than through `walkOf`'s analytic closed forms, and they route through ONE RECORD-LEVEL PREFLIGHT (`momentPreflight`, spline design §5.2): `validateMomentFieldsWithPoll` runs on ONE `freeformWork` for the whole `ProfileRecord` — the ceiling bounds a record's total work, never each segment's, so a record of individually cheap curves cannot be unaffordable in aggregate and still measure. That counter is a PARAMETER, kept on `momentPreflight.work`: every `evaluatorIntegrals*` entry takes it, so an operation that already opened one for this record (`Document.Extrude`/`Document.Revolve` do, at their area falsifier) hands the same one to the build's own preflight and to every walk after it, which then spend what is left rather than a second full ceiling — and `validateFreeformMomentSegment` levies on it every charge one SEGMENT owes — the conversion, the re-anchoring (`chargeFreeformShift`) and the integration (`chargeFreeformSpans`) — all before the reconstruction runs. The RECONSTRUCTION's charge is levied by `validateMomentFieldsWithPoll` itself (`chargeFreeformReconstruction`), once, over the whole checked record, because the arrangement it pays for is global; it returns the per-arrangement charge `momentRecordMatchesSketch` then levies for each candidate profile before re-arranging the scene to authenticate it, so a record can clear the preflight and still be refused inside the pass, and every arrangement is paid for before it runs. The converted chains are carried in the preflight's `freeformPlan`s and the moments pass integrates THOSE (`regionIntegrals.add` shifts and accumulates, converting nothing and charging nothing); `addAnalytic` is the section audits' entry, which take no plan because `requireAnalyticWalk` refuses every free-form walk at their own `walkOf` call sites. The reconstruction creates the spline/closed-spline/NURBS entity so sketch still decides region topology, with an all-equal NURBS weight vector normalized to ones for that reconstruction ONLY (`normalizeReconstructionWeights`) — equal weights cancel, so it is the same curve, and the raw magnitude would overflow sketch's own derivative. The positive-area gate (`requirePositiveArea`) reads the region's exact rational wherever there is one, so an area that is strictly positive and underflows float64 publishes the bounded zero the accumulator holds instead of refusing; `Centroid` reads the SAME accumulator (`exactCentroid`), dividing the exact first moments by the exact area over rationals and rounding each quotient ONCE — spline design §3's rule for the centroid too — so that underflowing section still reports the coordinates already in hand, and the bounded-float division serves only regions a circular contribution retired the accumulator for. Every other free-form kind is `ErrUnsupported` with its own Table R reason. |
| `spline_bezier.go` | The exact reduction of `docs/spline-design.md` §5.1: a recorded free-form curve becomes piecewise polynomial Bézier control points over `math/big.Rat`, with no rounding on the way — control coordinates AND knots are floats hence exact rationals, and knot insertion is a rational convex combination, so the spans ARE the curve. Every number the conversion reads is UPSTREAM's own float taken exactly, NEVER re-derived: `clampedUniformKnots` lifts the vector `geom.ClampedKnots` returns (whose interior knots are the FLOATS `float64(j)/float64(n−3)`, not the rationals `j/(n−3)` they round — rebuilding them integrates a curve sketch does not define and turns an honest `Approximate` into a false `Exact` wherever `n−3` is not a power of two), while `uniformKnotDemand` derives only that vector's SHAPE from the control count so the charge can precede the read. `clampedBezierSpans` is Boehm insertion raising every interior knot to multiplicity `degree` (serving `SplineSeg` through those lifted `geom.ClampedKnots` floats and `NURBSSeg` through its recorded knots), and `bezierSliceCount` PROVES the stride-degree slicing's own precondition on the post-insertion knot vector before a span is cut — consecutive spans share their boundary control point only while every interior knot sits at multiplicity EXACTLY `degree` and the control count strides into whole spans, neither of which a divisibility test on the control count establishes, so a record already past `degree` (needing no insertion at all) or one whose boundary knot is over-clamped refuses instead of being sliced across a stride it does not have. The SENTINEL is a fact about the curve, not the slicer (§5.1, Table R rows R12/R13): `brokenKnot` reads the two one-sided limits — the recorded control points `P_j` and `P_{j+m−degree}` — and a difference is `ErrDegenerate` (the curve breaks apart, so no such body exists) while identity, and every structural stride miss, is `ErrUnsupported` (the body exists and this evaluator cannot slice it). Equality is exact, never a tolerance. `closedSplineBezierSpans` uses the closed-form uniform identity per cyclic span, matching `geom`'s own `P[i..i+3]` indexing. Only Tier A converts: a RATIONAL `NURBSSeg` refuses (its spans are not polynomial), as do `FitSplineSeg` (sketch owns the interpolation solve), `EllipticalArcSeg` (§2.2's self-inconsistent record) and the Tier B kinds, each with its own reason. The recorded range carries TWO separate refusals and they are decided at different depths. `requireFiniteFreeformRange` is `ErrNotFinite` — the core §12 sentinel for a non-finite input — and it runs at the TOP of `freeformBezierSpans` over `freeformSegmentRange`, ahead of the kind dispatch, so every one of the seven free-form kinds reports it; deciding it inside the Tier A arms alone leaves the other four answering a NaN range with their own kind reason. `requireFullFreeformRange` stays on the three Tier A arms and rejects a trimmed range the seam could never have produced, so R2/R6 and the Tier B reasons keep winning over it exactly as Table R states them. Every O(1) SIZE refusal precedes every content scan (`validateNURBSSegmentSizes` before `validateNURBSSegmentContent`, the control-count floors before `ratPointsOf`), so a record that cannot be well formed at any content refuses in constant time however many control points it holds. `freeformWorkLimit` is the fixed R7 ceiling on ONE RECORD's free-form work across a whole OPERATION — not each segment's and not each pass's — since public `ProfileRecord` methods take no context; `newFreeformWork` is the one place a counter is opened, so every new one is a new full ceiling and the mint sites are countable, and every charge against it is a CONSERVATIVE PREFLIGHT levied before the work allocates: `chargeRationalLift` for the recorded arrays — computed from SLICE LENGTHS alone (`2·controls + knots + weights`, every array a preflight pass walks) and levied BEFORE the per-element content scan and the equal-weight tier test, which is what bounds them under the CONSTANT-FACTOR invariant it owns: every element-touching pass in the preflight is a SINGLE walk over one array whose length is a term of the charge, so the work is at most K times the units levied and at most `K·freeformWorkLimit` record-wide — K rises when a validator of that shape is added and the work can never become unbounded, while a pass over an uncounted array or a superlinear one is outside the invariant and owes its own charge (a scan ahead of every charge is unbounded and uncancellable); the invariant is backed by a measured boundary regression on the worst admitted record rather than a per-pass accounting identity; a segment's tier is therefore decided before its CONVERSION charge rather than before every charge, so every record whose SIZE fits the ceiling still reads its own Tier C reason and only a record refused either way reports R7 instead (§5.2) — and `clampedConversionCost` for the whole insertion pass — quadratic, because one insertion scans and copies both whole vectors and each insertion lengthens them, and per-target on top of that, because EVERY `knotMultiplicity` probe is charged including the ones that find the target already at `degree` and insert nothing (a degree-1 record with thousands of distinct interior knots is that shape, and its scans are quadratic work an insertion-only charge reads as nothing) — so a dense record refuses at the ceiling instead of running for hours inside it. The conversion charge is reserved BEFORE the rational lift, never after it: each caller reads its own `knotInsertionDemand` from sizes and from data it already holds as floats (`uniformKnotDemand` reads the `geom.ClampedKnots` vector's shape off the control count without asking for the vector, `floatKnotDemand` walks the RECORDED knots, a closed spline owes no insertion at all), so `clampedBezierSpans` charges nothing itself and a record hopeless from its control count alone refuses without allocating one `big.Rat` — the open-spline charge being quadratic, a refused record would otherwise allocate orders of magnitude more than any accepted one. `freeformReconstruction`/`reconstructionOf`/`chargeFreeformReconstruction` are the separate charge for the SKETCH RECONSTRUCTION (`momentRecordMatchesSketch`); nothing in the conversion or integration charges speaks for it, so without it a kind whose conversion is linear cleared the ceiling and then spent seconds of uncancellable time inside a public measurement method. That charge is the RECORD's, because the arrangement is GLOBAL: sketch chords every source and then tests every PAIR of chords in one loop over the whole scene, so the charge is a saturated quadratic in the record-wide chord TOTAL and a sum of per-source squares drops every cross-source pair. `reconstructionChords` restates sketch's own `sampleParams` table (`freeformChordsPerControl` 16 per control with a `freeformChordFloor` of 64 — an OPEN spline per SPAN, so per control less its degree; `analyticChordsPerTurn` 256 for a circle and `arcChords` for an arc's share of a turn; one for a line), and EVERY source counts, analytic ones included, since a free-form-only total says nothing about the pass the arcs beside a spline are arranged in. The pass runs the arrangement more than once — once to list the candidate profiles, once more per candidate `RecordProfile` authenticates, and the whole pass again on the rescaled record — so the two whole-scene arrangements validation always runs are levied ONCE at the record-level preflight and each candidate's own re-arrangement is charged on the same counter immediately before it runs, which is what keeps the ceiling a bound on the pass rather than on a prediction of it. `costAdd`/`costMul` saturate at `freeformCostCeiling`, so an oversized shape refuses rather than wrapping to a small charge. |
| `spline_length.go` | `docs/spline-design.md` §6.1: a free-form curve's arc length is never exact (a square root has no polynomial antiderivative), so it is BRACKETED — the chord below, the control polygon above, closed by exact de Casteljau bisection (`dyadicSpan.split`, all rational). The subdivision runs in the ONE form its own arithmetic has: bisecting introduces nothing but powers of two, so a split value is a `big.Int` numerator beside a binary exponent (`dyadicPoint`) and a midpoint is one integer addition and one exponent increment — no common factor to strip, where a normalising `big.Rat` pays two GCDs per midpoint to re-derive a denominator it already knows. A converted control point is NOT dyadic in general (Boehm insertion divides by a knot difference, the closed-spline identity by 3 and 6), so that constant is factored out once per span: `dyadicSpanOf` re-expresses the span over the least common multiple of its own denominators, which the whole subdivision then shares and the exponent carries everything else. `dyadicSpan.squaredDistance` is the single boundary back to `big.Rat`, at the leaves where the bracket is actually decided. Only the final square roots are floats, each rounded OUTWARD against an exact `big.Rat` comparison (`ratSqrtDown`/`ratSqrtUp`), so the interval encloses the true length whatever the platform's `sqrt` does. Their seed is `ratSqrtSeed`, which scales the exact rational by an EVEN power of two into `[0.5, 2)`, roots it there and scales back by half the exponent — reading the seed off the rational ROUNDED TO A FLOAT is what made scale a refusal, since a subnormal squared distance keeps a few significand bits and an overflowing one keeps none, either seed landing far outside the `sqrtAdjustLimit` walk. The seed is never the proof, so a better one only removes false refusals. `freeformArcLength` reports the midpoint and half width, so a free-form length NEVER claims a zero bound: a zero half width is a REFUSAL, and the one shape that reaches it — a control net collapsed to a single point, whose control-polygon upper bound is zero — is `ErrDegenerate` (Table R R14), the same answer `freeformDegenerate` gives the identical record on the moments path. The other refusal is R15 (`errFreeformLengthUnrepresentable`): an enclosure whose upper bound runs past `MaxFloat64` has no float64 interval to publish, `ErrUnsupported` because the curve exists and this evaluator cannot state its length — never `ErrNotFinite`, whose subject is a non-finite INPUT while every coordinate reaching a bracket is finite. `freeformLengthDepth` is 10 and FIXED: the bracket has no target of its own — nothing downstream compares an arc length against a caller tolerance — so no measured-target loop can stop, and §6.1 puts that loop where a target exists (§6.4's build gate, §6.1.1's product enclosure) under a hard iteration cap. A fixed depth promises NO relative width, and the reported half width is the enclosure `freeformArcLength` MEASURED at that depth, never a figure read off the asymptotic 4x-per-level rate: an ordinary cubic's relative half width is 1.9e-7 while the widest span the bracket's own preflight admits (32 controls) measures above 2e-5. `freeformBracketCost` is the per-span R7 preflight, charged before the first split allocates: depth `d` makes `2ᵈ−1` splits, each blending all `n(n−1)/2` de Casteljau pairs, so the charge reads the span DEGREE and not merely the leaf count — which is what keeps a single high-degree span from subdividing for hours inside the ceiling. `downRound` is `upRound`'s mirror, so a sum of lower bounds stays one. |
| `spline_moments.go` | The exact integration of `docs/spline-design.md` §5.1 over those spans, reusing `clearance_poly.go`'s `ratPoly` rather than forking a second polynomial type: `rpFromBernstein` converts one coordinate's Bézier control values to monomial form as the literal Bernstein sum, `rpIntegral01` is the exact ∫₀¹, and `exactFreeformMoments` evaluates the same Green's-theorem forms the line path uses (so the two are checkable against each other on a degree-1 span), negating every signed result when the recorded range order says the walk runs backwards. `freeformSpanCost` is the span's own conservative preflight, charged before a coefficient is allocated: integration cost is CUBIC in the span degree while the span is only `degree+1` points long, so a charge read off the control count admits an arbitrarily wide span. `chargeFreeformSpans` is the SINGLE owner of a whole chain's integration charge: the record-level preflight in `moments_validate.go` levies it — ahead of the sketch reconstruction that samples the curve — and `exactFreeformMoments` then integrates the chain that charge already paid for, consulting no counter of its own. `addFreeform` hands each curve's exact rational to `moments.go`'s REGION-level accumulator, which is what the published float is rounded from — once, after the complete signed sum — so the reported bound is that single rounding: zero, hence `Exact`, exactly when the rational is representable (§3), never unconditionally. |
| `topology.go` | The topology model (evaluator §3): `Body`→`Lump`→`Shell`→`Face`→`Loop`→`CoEdge`→`Edge`→`Vertex` with the core §6/§6.1 accessors, including `Loop.CoEdges()` as the directed boundary and `Loop.Edges()` as its undirected compatibility view. `CoEdge.Edge()` returns the shared edge; `Start()`/`End()` follow the loop walk; `IsForward()` reports whether that walk matches `Edge.Start()`→`Edge.End()`. The model also owns sealed `Surface` (`Plane`/`Cylinder`/`Cone`/`Sphere`/`Torus`/`Faceted` — the honest boolean-output flag, carrying its proven boundary-displacement `Bound`; `SurfaceKind` discriminants) and `Curve` (`Line3`/`Circle3`/`Arc3`/`FacetedCurve`) sets, multi-role `Face.Origins()`, bounded `Vertex.Position()`. `Edge.IsConvex` is the WALKED-BOUNDARY convexity, never the 3D material angle: the profile is walked with the material on its left (outer loop CCW, holes CW), a junction edge reads the turn between its two walls, a rim edge reads the sense of the wall it runs along (a circular wall by its own turn — CCW convex, CW concave — a straight wall by its loop's role), and a partial revolve's on-axis cap edge is convex under a half turn — so a HOLE's rim edges are concave, as are those of a concave round on the outer loop, even though the material there is a chamferable quarter-turn wedge. `Edge.Length()`/`Face.Area()` key exactness off their own proven bound: zero only for exactly representable analytic results; analytic float evaluations and boolean results carry their own or composed bound, and a float-computed length or area is never Exact. Whether a face is FLAT is `Face.isPlanar()`, not the surface tag: an analytic `Plane` is by construction, and a `Faceted` face is when every source it descends from was, so a second-generation all-planar boolean's straight rims answer instead of refusing (a `Faceted` rim on a genuinely CURVED source still refuses — its true length is unboundable). Bodies are immutable after construction. |
| `document.go` | `Document` (`New`/`Bodies`/`Recipe`), the atomic commit tail (record→evaluate→commit; a failed evaluation leaves recipe and document untouched), retire/`requireLive` gates, `Body.Placed` (re-evaluates the payload under the composed rigid motion, retiring the receiver), the non-consuming copies `Body.Duplicate`/`Body.PlacedCopy` (shared `copyUnder`: same payload re-evaluation, but the source `StepRef` is a depended-on `Inputs` ref never in the consumed set, so the source stays live — a duplicate is the identity motion recording no `Placement`, a placed copy records its motion), and `magnitudeIn` (Kind/finite/non-negative validation). |
| `extrude.go` | `Document.Extrude` (evaluator §5) PLUS `segmentWalk` and its `walkKind` vocabulary, which every feature, survey, audit and trim reads a boundary segment through. `walkKind` (`walkLine`/`walkCircular`/`walkFreeform`) replaced a line-versus-circular BOOLEAN because a free-form walk is neither, and a two-state flag left every "not circular" branch silently sweeping a spline as the straight line between its endpoints. Reads go through `isLine()`/`isCircular()` — never `!isCircular()` meaning "line" — and every `walkOf` call site a free-form segment can reach gates through `requireAnalyticWalk` (`ErrUnsupported`, its reason a `docs/spline-design.md` Table R row) so no consumer without a free-form construction can be handed one; the one ungated site is `validateMomentWalk`, which `moments_validate.go` reaches only after diverting every free-form kind to the exact integrator. `walkOf` takes the RECORD's `*freeformWork` and mints none: the R7 ceiling is one record's across a whole operation (spline design §5.2), so `Document.Extrude`/`Document.Revolve` open one counter at their area falsifier and thread it through the build's own preflight, `buildLoopSides`, `profileCoordinateUpper` and `boundaryExtremes`, while the paths with no preflight in hand — a placement's re-evaluation, a modify build's rewritten section, the cup build, tessellation chording, the surveys, the section audit, an `extentAlong` reading — open exactly one for the record walk they are about to make. A free-form resolution handed NO counter is unbounded, so it refuses (`errFreeformWalkUncounted`) rather than quietly mint one. `freeformWalk` resolves a Tier A segment: endpoints off the converted chain (a Bézier interpolates them exactly), tangents from the hodograph's end legs, the length from §6.1's proven bracket charged against that same counter (so `lengthBound` is always positive — `freeformArcLength` owns both of that bracket's refusals, a control net collapsed to a single point being `ErrDegenerate` and an enclosure past `MaxFloat64` being `ErrUnsupported`, rather than resolve either into a walk), and convex-hull envelopes for `coordUpper`/`lengthUpper`; `axisRadiusUpper`/`axisMomentUpper` stay zero because revolve refuses first. The rest: gates via `RecordProfile`, `WithTaper` recorded-then-`ErrUnsupported`, extent resolution to the signed interval — `Distance`/`Symmetric`/`TwoSided` plus the body-relative stops (`ThroughAll`/`ThroughAllSide`/`ToFace`, resolved through `stops.go` with the stop bodies recorded as `StepRef`s in the step's `Inputs`) — and the analytic prism evaluator — shared-edge manifold topology, closed-form volume/area/centroid with proven bounds (Exact only when those bounds are zero), exact directional-extreme bounds (`prismPayload.extentAlong`, the arbitrary-direction extent the through-all stop reads). A payload built from a construction whose record is only within a proven displacement of the section it DENOTES carries that displacement as `sectionDelta` (`docs/prism-boolean-design.md` §7 — the analytic prism boolean is the only construction that sets it, zero everywhere else), and `evalPrism` composes it into every reading: `bounds.go`'s `sectionDisplacementArea` into the region area, hence the volume through the height and each cap once; `sectionDisplacementLength` into each walk's length, hence the wall areas and the perimeter; δ into the centroid's plane-local source term and its geometry envelope alike; δ into each junction vertex and into `Box` (outward on every face), which is Exact only while δ is zero. The payload is self-describing for re-evaluation: `prismPayload` also carries its own blend-role descriptors (`blendSegs`/`blendKind`, empty for a plain extrude), and `evalPrism` re-applies `addBlendRoles` from them under the build's own `StepRef`, so a copy or placement of a filleted/chamfered prism re-mints its own `fillet(i,j)`/`chamfer(i,j)` roles (modify §9). |
| `revolve.go` | `Document.Revolve` (evaluator §6): the sealed `Axis` vocabulary (`SketchLine`/`ConstructionAxis`/`EdgeAxis` + tagged codec; `Step.Axis` keyed to `OpRevolve` on the wire like `Angular`), the current §6 axis gates (coplanar with the profile plane; region in one closed half-plane, either side; interior per-walk tangency is `ErrDegenerate`) and the target axis-incidence rule (`docs/evaluator-design.md`: every on-axis point has exactly one off-axis walk end plus one on-axis `LineSeg` end from the same loop, so endpoint-segmented horns refuse), angular-extent resolution to the sweep interval (a 2π magnitude IS a full revolution, past it is `ErrDegenerate`; the body-relative `ToFaceAngular` stop resolves through `stops.go` AFTER the axis gates — it needs the resolved axis — with its stop body recorded in `Inputs` before the axis ref, deduplicated), `EdgeAxis` resolution (`resolveEdgeAxis`: the named body live in this document, the selector resolving to exactly ONE LINEAR edge — only the built-in `*EdgeQuery` is valid, a foreign implementation (including one embedding `*EdgeQuery` to promote the sealed `selector()` marker) is malformed input rejected as `ErrDegenerate` before `SelectEdges` runs; a failed own explicit assertion passes through unchanged, any other non-one count is rewritten to a `SelectionError` wrapping `ErrCardinality`, `Expected "exactly 1"` via `impliedOneEdge`, the core §12 implicit exactly-one; a non-linear edge is `ErrDegenerate`; the axis runs start→end vertex of the resolved edge, the step records the query with the body as its producing `StepRef` in `Axis` AND `Inputs`, and the body is depended on, never consumed), and the analytic revolve evaluator — `Cylinder`/`Cone`/planar-annulus/`Sphere`/`Torus` walls per segment kind, caps only on partial sweeps (a full sphere/torus face carries no loops), latitude-circle seam vertices, Pappus-based volume/area/centroid with proven bounds (Exact only when those bounds are zero), exact cylindrical-extreme bounds (`revolvePayload.extentAlong`). |
| `stops.go` | The body-relative stop resolution (evaluator §5/§6/§11 increment 2, core §8.1/§6.2). Shared gates mirror `EdgeAxis`'s: the named body live in THIS document (`resolveStopBody`: nil/wrong type `ErrDegenerate`, `StepRef` `ErrUnresolvedBody`, foreign/retired `ErrForeignBody`/`ErrRetiredBody`), the face selector under the implicit exactly-one (`selectStopFace`: decad owns the selector vocabulary, so only the built-in `*FaceQuery` is valid — a foreign implementation, including one that embeds `*FaceQuery` to promote the sealed `selector()` marker, is malformed input rejected as `ErrDegenerate` before `SelectFaces` runs; a selector whose OWN explicit assertion failed passes through unchanged, any other non-one count — unasserted zero/many or a satisfied assertion ≠1 — is rewritten to a `SelectionError` wrapping `ErrCardinality` with `Expected "exactly 1"` via `impliedOneFace`; typed-nil query `errNilSelector`). `ToFace` (`resolveToFace`): the stop face must be PLANAR with its normal parallel to the sweep — the §5 build table's cap is planar and perpendicular — else `ErrUnsupported` (staged, never a wrong prism); a face coplanar with the sketch plane, a side-mismatched face on a `TwoSided` side, or an offset pulling the stop behind the plane is `ErrDegenerate`; standalone sense comes from the target face, sides from the side they occupy. `ThroughAll`/`ThroughAllSide` (`resolveThroughAll`): the stop is the farthest far side of every live body the sweep meets — "meets" judged EXACTLY on each body's closed-form `extentAlong` the sweep direction (material strictly beyond the sketch plane in the travel sense; the lateral footprint is deliberately not consulted — deciding it exactly is boolean-grade machinery, and a guess would fabricate or drop a recorded dependency); no body in the path is `ErrDegenerate`. `ToFaceAngular` (`resolveToFaceAngular` on the `angularStops` ctx): the stop face must be planar AND its plane contain the revolve axis (else `ErrUnsupported`), its material in ONE half-plane — located by exact boundary probes (vertices + arc midpoints/circle antipodes, `boundaryProbes`) with a closed-form arc axis-crossing audit — a both-sides face or one in the profile's own half-plane is `ErrDegenerate`; a standalone stop takes the nearer way around, a `TwoSidedAngle` side the side's own sense. Inputs assembly: named-extent refs in extent order, through-all stop bodies in stop order after them, the axis ref last, `dedupRefs` first-occurrence; `recordExtent`/`recordAngularExtent` substitute `StepRef`s + deep-copy selectors into the step. |
| `fillet.go` | `Body.Fillet` (modify design §6, PR 1) PLUS the SHARED modify machinery Fillet and Chamfer both use. Fillet rounds the convex or concave lateral edges of a straight prism receiver (Table R, R1). The reduction is modify §2 — a lateral edge is a CORNER of the recorded 2D section, and rounding it is a rewrite of that section into a tangent arc of radius `r`, fed back through `evalPrism` so mass properties, tessellation and the surveys come for free (§10, Table D). Gate order is §4's: S17/S15/S13 pre-gates, S16 (the selector, resolved against the live receiver), S3 (the receiver is a `prismPayload`, else `ErrUnsupported`), S1 (every selected edge maps to a section corner via `matchCornerBudget` — a cap edge matches nothing, the vertex-blend problem, `ErrUnsupported`), then per corner S4 (a smooth/cusp corner is `ErrDegenerate`) and S5 (`computeFillet`: the two carriers' material-side offsets are intersected for the center nearest the corner — the offset sign is convex/concave, `insideSign` reads the circular walk's own sense; a non-positive circular offset radius or non-meeting offsets is `ErrDegenerate`). The blend is exact closed form (line/line, line/arc, arc/arc), NEVER a residual: `carrierOf`/`offsetOf`/`intersectOffsets`/`footOn`/`cutbackOn`, the arc's walk sense from the tangent rule. **The SHARED corner-rewrite type is `cornerBlend`** (feet `fA`/`fB`, per-walk `cutbackA`/`cutbackB`, and a `connector CurveSegment` — a fillet's tangent `arcSegment`, a chamfer's chord `LineSeg`); `prismCornerLoopsBudget`/`matchCornerBudget`/`walkSegment`/`rewriteProfile`/`rewriteLoop`/`addBlendRoles`(`kind`) and the whole `fillet_audit.go` audit all read only that shape, so Chamfer forks none of them. `rewriteProfile`/`rewriteLoop` re-emit each loop's coalesced walks trimmed to the feet with the connector inserted (`arcSegment` records a CW walk as a reversed-range `ArcSeg`); `addBlendRoles(body, ref, segs, "fillet")` gives each blend wall its second `fillet(i,j)` role beside `side(i,j)` (Table B, B1). The Step records `OpFillet`, the receiver `StepRef`, the deep-copied selector and the radius `Value`, `Opts` nil; the receiver is retired. `Shell` and cap-edge/non-prism receivers stay `ErrUnsupported`. |
| `chamfer.go` | `Body.Chamfer` (modify design §7, PR 2): bevels the convex or concave lateral edges of a straight prism receiver (Table R, R1) — the fillet's sibling under the §2 reduction, with ONE new piece of geometry: the corner is cut off by a straight CHORD between the two setback feet, so the bevel wall is a `Plane`, never a `Cylinder`. Signature is a setback DISTANCE `d` (not a radius). Everything downstream is SHARED with `fillet.go` via `cornerBlend`: `prismCornerLoopsBudget`/`matchCornerBudget`, `rewriteProfile`/`rewriteLoop`, the §5 `auditRewriteBudget`, `evalPrism`, and `addBlendRoles(…, "chamfer")` for the second `chamfer(i,j)` role. Chamfer-only code is `computeChamfer` + `setbackFoot`: each walk is set back an equal arc length `d` measured ALONG the boundary curve — `d` back from the arriving walk's end, `d` forward from the leaving walk's start (a line steps by its unit tangent, an arc by the angle `d/R` in its own turn sense) — and the feet are joined by `LineSeg{fA→fB}`. Gate order is §4's, identical to the fillet EXCEPT there is **NO S5**: a chord exists between any two distinct feet, so the fillet's no-blend-centre refusal has no chamfer case (Table B, B1). S4 (`computeChamfer`: a smooth/cusp corner is `ErrDegenerate`); an over-large setback whose feet run past a wall's far end is the SHARED audit's S6 (`ErrUnsupported`), never clipped. A convex corner's chord removes a wedge, a concave corner's fills one in — the same construction, `IsConvex` not an input. The Step records `OpChamfer`, the receiver `StepRef`, the deep-copied selector and the distance `Value`, `Opts` nil; the receiver is retired. A CAP-edge selection is not refused here: a selection covering every geometric edge of one or more complete cap loops routes to `capblend.go`'s cap-loop chamfer (`classifyChamferSelection`), and only a partial or lateral-mixed cap selection is SX4 `ErrUnsupported`. `Shell` and non-prism receivers stay `ErrUnsupported`. |
| `fillet_audit.go` | The modify §5 audit of a modify op's rewritten section — SHARED by Fillet and Chamfer (`auditRewriteBudget` takes the common `cornerBlend`, its only op-specific input the per-corner cutback S6 sums; its error messages are op-neutral), run before any face is built in §4's order: S8 (orientation — `loopSignedAreaBudget` sign per loop must not flip or collapse, `ErrDegenerate` — UNLESS a local cutback overrun explains the flip, which defers to S6, since Table S makes an overrun `ErrUnsupported` even when it also flips the loop), S6 (no walk consumed by its own corners — `loopOverrunBudget`: a walk whose two ends' cutbacks reach or pass its far end, else `ErrUnsupported`; the two-fillets-one-short-wall case AND a single corner whose own setback runs past an adjacent walk, computed up front so S8 can consult it), then S7 (`crossingAuditBudget`: every non-adjacent segment pair tested for an interior crossing — `segCross`, line/line, line/arc, arc/arc closed form — AND for BOUNDARY CONTACT, a tangency or a shared boundary point with no interior crossing — `segMinDist` ≤ `contactFloor`, the closed-form minimum distance between the two closed primitives against a SCALE-ANCHORED threshold, δ = ε·D with ε = 1e-9 and D the section's (u, v) bounding-box diagonal — `sectionBBoxBudget` folds each ARC's own extrema (the cardinal points cU±R/cV±R its walk reaches, NOT the endpoint chord an arc bulges past) so D is the true section diameter — the SAME diameter-anchored noise floor verification design §4 uses for a length (`gapWithinTolerance`), reject-only so a gap within δ is refused and a gap comfortably above it builds, never a fixed absolute band; both are `ErrUnsupported`). Then S9 (nesting) is COMPUTED, not discharged by construction (`nestingAuditBudget`, §5 test 4): S7 leaves the loops strictly disjoint, but disjoint is nested OR mutually exterior and S8 (each loop's own signed area) reads no relative position, so a large fillet can shrink the outer loop past a near-corner hole, leaving it OUTSIDE the rounded material — disjoint from every segment yet no longer contained. `nestingAuditBudget` classifies one point of each hole against the outer loop and each other hole by the ray-parity walk with direction retries `survey2d.go` runs (`loopContains` over `rayCrossings`, `elemOf` mapping walks to `surveyElem`): the outer loop must be PROVEN to contain every hole and the holes proven mutually exterior. An undecidable containment is S9 `ErrUnsupported` (a build-time audit has no `Suspect` to fall back on); a hole PROVEN outside the outer loop, or nested inside another hole, is nesting decidably broken — the fillet consumed the region the caller's section lived in, so no such body exists: an S8-family `ErrDegenerate` (§1 existence test). Shell REUSES this same audit on its offset section (`auditOffsetSectionBudget`, `shell_offset.go`) — an empty fillet map makes S6 a no-op (an offset mints no cutback), so a shell's S7 IS the S11b merge event. |
| `shell.go` | `Body.Shell` (modify design §8, PR 3): removes the selected cap faces of a straight prism and lines the rest with a wall of thickness `t` (Table R, R1). `ShellSense` (`Inward`/`Outward` + named text codec; `WithShellSense`, the one `StepOpts` field this increment fills, `ShellOpts{Sense}` in `recipe.go`). Gate order is §4's: S17/S15/S14 pre-gates, S16 (the face selector), S3 (a `prismPayload`, else `ErrUnsupported`), S2 (`classifyRemovedCaps` — every removed face is `capStart`/`capEnd`, else a side wall `ErrUnsupported`), S18 (inward `sectionInradius` work stays within §8's fixed limit), S10 (the inradius section limit, and the sweep height for a kept-cap cup, both reached ⇒ `ErrDegenerate`), then the exact offset (`offsetProfile`), the §5 audit (`auditOffsetSectionBudget`), and the build. A both-caps hole-free shell is a TUBE (`evalTube` → `evalPrism` over the annular section `{Outer, reverse(Hole)}`, a `prismPayload` — first-class downstream); a one-cap shell is a CUP (`cupPayload`), for any `k ≥ 0` — the floor joins every band into one lump. S12 (both-caps HOLED, `1 + k` disjoint lumps) is `ErrUnsupported`; the Step records `OpShell` + the deep-copied selector + `t` + `ShellOpts`, retiring the receiver. |
| `shell_offset.go` | The exact per-feature section offset (modify §8) and the §5 audit wrapper. `offsetProfile`/`offsetLoop`: `P ⊖ t` (inward, s = +1) / `P ⊕ t` (outward, s = −1), each loop offset in its own sense — a line to a parallel line, a circle to a concentric circle (`offsetRadius` = `R − s·insideSign·t`, non-positive ⇒ a dropped segment, S11a `errOffsetDrop`), a corner closing with a MITER (offset carriers intersected via the fillet's `offCurve`/`intersectOffsets`) or an ARC of radius `t` about the corner point — an arc exactly when `sign(cross) == −s` (inward reflex, outward convex), walked CCW outward / CW inward so its tangent continues the walk. S11a `errOffsetDrop` takes a second shape here — a whole LOOP dropped (a polygonal hole narrower than `2t` offset outward, whose erosion is empty): the corner joins overshoot every walk so each runs backward (`walkOffsetConsumed`), and the eroded loop keeps its walk sense so S8 cannot see it — decided per walk as the offset is built, before any section is audited. A non-closing miter is S11 `errOffsetTopology`. `reverseLoopRecord` walks a loop the other way (an offset outer becomes a tube's hole). `auditOffsetSectionBudget` runs the shared `auditRewriteBudget` with an empty fillet map. Both drop/topology errors are `ErrUnsupported`, caught at construction (antecedent to the audit, §4). |
| `shell_cup.go` | `cupPayload` + `evalCup` (modify §9, Table B B5/B6): the one new payload — two co-directional prisms over one plane, the outer region O and cavity C sharing a rim at the open end (`zOpen`) and a floor slab `[zOuter, zCav]` at the closed one. `cupPayloadFor` derives the three planes from the receiver interval, the sense and which cap was removed (a removed bottom is the mirror). `evalCup` builds outer walls over EVERY loop of O (`buildLoopSides`, role `side(i,j)`; a hole of O is a tunnel through the whole body), cavity walls over every loop of the REVERSED C (`buildLoopSidesAs` with the void's inverted orientation — the void's outer boundary a hole in the solid, each of its own holes a solid POST — role `shellSide(i,j)` via `renameCavityRoles`), the kept cap `capStart` and the pocket floor `shellCap` (each carrying one loop per region loop), and one `rim(i)` per loop (`1 + k` of them — the removed cap's plane trimmed to the band between loop `i` of O and loop `i` of C, the bigger boundary being O's for the outer region and C's for a post rim) — one manifold watertight shell, one lump, `IsVoid` false. Measurements are Exact per §10: volume `A_O·h_o − A_C·h_c`, area `2·A_O + perim·h` (perimeters summed over all loops), the mass-subtracted centroid, the outer prism's bounds. `placed` re-evaluates under the composed motion (evaluator §8). A through-all stop reads the cup via `extentAlong`, which returns the OUTER prism's directional extent — the cavity being interior (D5 works). Every cup — hole-free OR holed — TESSELLATES (`tessellateCup`, D4) and answers the wall (`cupWall`, D1), undercut (`cupUndercuts`, D2), and `MinRadius` (`cupMinRadius`, D3) surveys from the exact payload. `cupWall` reconstructs and audits the offset morphology, returning Exact zero for an allowance-qualified material pinch or the Exact shell thickness; a failed recheck stays `Suspect`. Each of the `1 + k` region loops is chorded once and shared by its walls, its floor cap and its half of its rim band, so the mesh is watertight by construction with every tunnel and post present; the other two surveys walk EVERY loop (`side(i,j)`/`shellSide(i,j)`), so a post's undercut is caught and a tunnel's concave radius read. Every rim band — including the bridge-collinear one (a rounded outer loop whose corner tangent points land on a sharp inner loop's edge lines, from an outward cup or a rectangular post) — triangulates into a watertight band; `requireClosedMesh` then PROVES the assembled mesh closed and refuses a cracked one rather than return it (`ErrDegenerate`), a safety net, never a wrong mesh. An invoked cup clearance pair remains staged as `Suspect` (no `cupPayload` in the kernel's payload switch — D6), while a box-disjoint cup pair stays `Sound`. |
| `capblend.go` | `docs/modify-reach-design.md` §8.3's cap-loop CHAMFER slice (§14 PR E): `capBlendPayload` (the receiver's unrewritten section, the single equal setback `d`, and which loop indices are chamfered on which cap — evaluator-private, RX4), `requireNotCapBlendReceiver` (SX10, called from `FilletContext`/`ChamferContext`/`ShellContext` before their `prismPayload` cast), `classifyChamferSelection` (RX1's second class: a selected edge set is either every lateral edge — the base path, unchanged — or every geometric edge of one or more COMPLETE cap loops; mixing the two classes or covering only part of a loop is SX4), and `buildCapBlend` (the SX6/SX7/SX12/SX13 gates in §4's own stage order — SX6 first, then SX7's band reach, then the stage-6 audit: `mixedOffsetProfile` offsets exactly the selected loops by `d` into the material and leaves the rest unchanged, `offsetLoopBudget`'s own drop is SX6 — re-sentinelled to `ErrDegenerate` by `wrapCapBlendDropError` at that one call site, since an empty cap contour means the body the caller named does not exist, while the same drop stays Shell's own S11a `ErrUnsupported` where a shell body does exist and only a trimmed-offset kernel is missing — and `auditOffsetSectionBudget` decides the rest through `wrapCapBlendAuditError`, which relabels each refusal with the §4 stage-6 row it came from and PRESERVES that row's own sentinel: a crossing/contact/merge refusal is SX7/SX12 (`ErrUnsupported` — the body exists and this evaluator cannot trim or merge it), while the base S8/S9 nesting refusal keeps `ErrDegenerate` through `%w` (the offset section is decidably broken, so no such body exists), the opposite existence claim — sound because SX12 audits the exact offset FAMILY, not the ruled patch the body builds: at axial fraction `s` the denoted miter locus is the parallel section offset by `s·d` (docs/modify-reach-design.md §8.3), a fact about that family alone, so disjointness at the full offset (`s=1`) certifies the whole family by the offset distance's own monotonicity), and `requireCapBlendLevelsSeparate` — SX13's AXIAL half, the sibling of `capblend_geom.go`'s radial `capBandRadius` on the band's OTHER directrix: the band's side level is its cap level moved axially by `d`, and a sweep tall enough that `z1 - d` is bit-identical to `z1` (or `z0 + d` to `z0`) collapses the two contours onto one level, so every patch is emitted FLAT in the cap plane and a `Plane`, which carries its normal with no bound, then asserts the chamfer's 45° taper as a fact about geometry that has none — which is precisely what DX7 reads. The volume is not what refuses it (the collapsed body reports the correctly rounded volume of the true chamfered solid, honestly bounded); the SURFACE is. It is a fact about the sweep interval and `d` alone, so it is decided once per chamfered cap, and it never overlaps SX7, which refuses a setback so LARGE beside the sweep that the band passes the far end where this refuses one so SMALL that the level it displaces does not move. SX7 is nevertheless the ONE row that can fire on the same input as another, and its position in the order is what keeps that input to one sentinel: a setback that empties the cap contour names a body that does not exist at any sweep height, so SX6 must decide it whether or not the sweep is short enough for the bands to meet — deciding the band reach from the sweep interval alone, ahead of the offset, lets the height alone pick the sentinel. `capBlendPayload.extentAlong` (DX5) is the interval its own patches attain, extremized over the pieces the build made: every boundary piece is ruled between two level curves — a trimmed side wall between the original loop at its own two straight-slab levels, a chamfer patch between the original loop at the side level and the offset cap contour at the cap level — and a linear functional on a segment is extremized at an endpoint, so extremizing those level curves is complete and every candidate is a point the body holds. The receiver prism's own extent is reusable in NEITHER form: padded by `d` it claims an attainment the body does not have (the cap contour offsets INTO the material and `[z0, z1]` is unchanged, so the body is contained in the receiver), and unpadded it still overshoots along a diagonal whose prism maximum sits at the very corner the chamfer removes. `stops.go` reads this as the sweep's exact stop coordinate AND as the "is this body in the path" test, so an approximation there both overruns the sweep and fabricates a dependency on a body it never meets — which is why `extentAlongWork` REFUSES (`ErrUnsupported`) rather than approximate wherever the winning extreme is held by the COMPUTED cap contour with a direction that reads its in-plane displacement, exactly as `prismPayload.sectionDelta` leaves `ThroughAll` staged. `extentBoundedAlong` is the reading underneath, returning the interval AND its proven displacement per CANDIDATE, so a contour that loses the extremization contributes nothing and a plate whose world-axis extremes are all recorded coordinates still answers exactly; `capBlendBoundsContext` is its one bounded reader. The cap-loop FILLET (§8.2) and `WithAsymmetricChamfer` (PR A) are not implemented here. |
| `capblend_geom.go` | The `capBlendPayload` topology builder (Table BX row BX3): `capOffsetJoins` mirrors `shell_offset.go`'s per-corner offset classification (miter vs. reflex arc) but returns the joins themselves for correspondence; `buildCapBand` builds one loop's chamfer band on one cap — a full closed circle is one whole-turn `Cone` patch, otherwise a `Plane` per straight wall and a `Cone` per circular one between the offset "cap contour" and the original "side contour", plus a Cone-with-corner-apex patch at each reflex corner (its apex the ORIGINAL corner point, unmoved) — sharing every side-level vertex/edge with the trimmed prism side wall (`buildLoopSidesAs`, called unmodified over each loop's own trimmed `[z0,z1]`) and mitered corners' vertices between neighboring patches, so the whole body is watertight by construction (every edge bounds exactly two faces). A reflex corner's cap-level connector is the arc of radius `d` about the corner walked CLOCKWISE — the corner's own reflex turn, never its `2π` complement, which is the arc on the far side of the corner and lands on a curve the offset never emitted — and the material it leaves lies radially OUTSIDE that arc, so the apex patch's cone has the VOID inside it: its outward normal points at the corner's own axis and toward the chamfered cap, and the radially-outward reading is not merely the wrong sign but EXACTLY perpendicular to the truth, since `dc = ds = d` stands the cone at 45°. `coneSurface` decides the patch's surface KIND on what the two stored radii ARE and never on how close they are: different radii name a `Cone` however small the difference, and `Cylinder` is reserved for exact equality — a tolerance there would answer a large radius with a small setback with a shape whose taper is gone, and the taper is precisely what DX7 reads off the surface's own `NormalAt`. `capBandRadius` is the ONE place a circular wall's cap-level offset radius is resolved, and it owns both of that offset's refusals: an offset reaching the centre is `errOffsetDrop`, and an offset whose radial change ROUNDS AWAY (`R -/+ d` bit-identical to `R`) is SX13's RADIAL half, `ErrUnsupported` — the body exists, its taper finer than float64 names at that radius. SX13's axial half is the same collapse on the band's other directrix and lives in `capblend.go`'s `requireCapBlendLevelsSeparate`, since it is a fact about the sweep interval rather than about any one wall. `buildCapBand` also returns the band's own contour displacement (`capBandResult.delta`, from `capblend_contour.go`'s `capContourDelta`/`capWholeCircleDelta`) rather than leaving each reader re-derive it through `loopContourDelta`, so the cap face area, the band volume and every patch's own area agree on the SAME value. Each patch `Face` also carries the area and bound its own `capPatchGeom` states (`setPatchArea` over `patchAreaOf`), so `Face.Area()` and `Body.Area()` agree; left unset a patch reports an EXACT zero area, a positively wrong assertion about a face the body's own area sum counts in full. That geometry now carries `contourAllow` too — one patch's own proven allowance (`bounds.go`'s `bandPatchAreaAllow`) for how far its area can differ from the ruled quad the offset denotes, computed once at build time from the patch's own held chord (its cap-level edge or connector arc) and slant (the larger of its two bounding slant edges), and folded into `patchAreaOf`'s returned bound — zero wherever the band's own `delta` is zero, which is what leaves an axis-aligned section's exact miters unchanged. Each patch's `capPatchGeom` records its angular window normalized to `th0 < th1` for the DX7 survey and keeps the walk sense that normalization drops in `sweepCCW`, which is the only place `capblend_moments.go`'s flux integral can read it. A regular wall's patch carries a SECOND window, `capTh0`/`capTh1` (`capWallSweep`, normalized by the SAME swap decision as `th0`/`th1` so the two stay paired corner-for-corner): the offset corner feet's own angles about the wall's exact centre, generally DIFFERENT from `th0`/`th1` at a non-tangential miter corner (docs/modify-reach-design.md §8.3) — the side directrix is the wall's own untrimmed sweep (DX7 genuinely reads it there), the cap directrix is the offset TRIMMED to the feet, and `capblend_moments.go`'s `patchRawFlux` integrates the straight-ruled patch between the two rather than a single shared-window cone sector; `patchAreaOf` does not — its own area stays the constant-slant frustum-sector formula, read against the trimmed cap-level sweep, with the gap from the two windows genuinely differing widening its BOUND (`windowSkew`) rather than its own integral. An apex patch (side radius zero) and the cornerless whole circle set `capTh0`/`capTh1` equal to `th0`/`th1`, so every circular patch reads through the one formula. It keeps a third fact that normalization cannot restate either: `wholeTurn`, set ONLY on the single whole-circle patch a cornerless closed loop builds, since a full period is a structural fact of THAT construction and no float window ever proves one (`fl(2π)` is not `2π`). `capblend_moments.go`'s flux bound is its one reader, where a full period makes the eccentric trig term's true value exactly zero. Patches mint only `chamferCap(c,l,p)` roles (§10/§11: no inherited provenance), `p` being Table BX's own "deterministic patch order in the result's `capBlendPayload`" — the patch's index in the band's patch slice, NEVER the wall or corner index, because `capOffsetJoins` puts corner `i` at `walks[i].start` and those two index spaces collide: one role on two faces collapses every last-wins reader (`facesByRole`, `FaceCreatedBy`, and the survey's own lookup into the payload's patch list) onto whichever face was built second. Ordinary side/cap roles are the prism's own. Every cap-level coordinate the band emits — each miter/reflex foot `Vertex`, each slant `Edge` down to the side-level apex, each wall's own cap edge, each reflex connector arc and the cornerless circle's whole circumference — is published through `capblend_contour.go`'s ONE contour displacement (`capContourDelta`, taken once per band before a vertex is built) rather than with a zero `bound` or an infinite `lengthBound`: the cap contour is COMPUTED by a float offset solve, so a zero there asserts an exactness the solve never had, and an infinite length is a wrong answer `selector.go`'s `LongerThan` reads straight off the raw field. The side-level apex carries `levelDelta` — the rounding of `capZ + matSign*d`, the same term `capBandVolume` charges for the identical level — so a slant edge is charged one displacement per endpoint. |
| `capblend_contour.go` | The cap contour's DISPLACEMENT (`docs/modify-reach-design.md` §8.4): the one proven upper bound on how far a cap-level point the build EMITS sits from the point the offset DENOTES, derived once per band by `capContourDelta` (and by `loopContourDelta` for the readings that hold no built band — `extentBoundedAlong`'s, which evaluates the same contour through `capLoopBoundary`). The proof is an ENCLOSURE and never an error model: `ivCarrierOf`/`ivIntersect`/`ivNearest` re-evaluate `shell_offset.go`'s `offsetCarrier` and `fillet.go`'s `intersectOffsets` — the same three cases in the same dispatch — over `ratInterval`s with every recorded coordinate taken EXACTLY, the only widening at the square roots, which round outward through `spline_length.go`'s `ratSqrtDown`/`ratSqrtUp`. Interval arithmetic is inclusion-monotonic, so the box holds the denoted point whatever the platform's `sqrt` and `hypot` did, and the displacement is the box's own greatest reach from the float point the build holds; nothing here assumes an ulp contract and nothing here can ADMIT a point, only state how far it may be. `ivNearest` reproduces `intersectOffsets`' own nearest-the-corner selection over intervals, dropping a candidate only where the exact comparison PROVES it is not the nearest and hulling every one it leaves undecided, so a near-tangency reports one honest wide displacement instead of a branch nothing decided. `errCapContourUnbounded` is SX14: a determinant interval straddling zero, or exact carriers that do not meet at all, leaves no box to report and the call refuses. The file also owns each cap-level edge's own length bound — `straightEdgeBound` (the square root's committed error against `ratSquaredDistance3`'s exact rational, plus one displacement per endpoint), `capApexArcBound` (an `atan2Interval` bracket on the computed feet's own turn plus `arcSweepAllow`'s `π·delta`, the arc length a foot displacement can account for), `capCircleLengthBound` (`2πr` against `piLower`/`piUpper` and the EXACT offset radius) and `capWallArcBound` (a regular wall's own cap-level sweep, bracketed via `atan2Interval` straight from the offset corner feet — capblend_geom.go's `capWallSweep` resolves the SAME feet into the float `capTh0`/`capTh1` the build holds, generally a DIFFERENT window than the wall's own recorded `th0`/`th1` at a non-tangential miter corner, docs/modify-reach-design.md §8.3 — plus `arcSweepAllow`'s turn the feet's own displacement can account for). |
| `capblend_moments.go` | `evalCapBlendContext` (the build orchestration: per loop, the straight-height slab via `buildLoopSidesAs` plus each chamfered cap's band via `capblend_geom.go`, then the two cap faces mixing offset and unchanged loop boundaries) and the §8.4 bounded mass-property integrals. The chamfered cap face's own area (`loopEnclosedAreaContext` over `capLoopBoundary`'s offset loop, feeding `startArea`/`endArea` and hence both `capStart`/`capEnd`'s `Face.Area()` and `Body.Area()`) is measured on the BUILT contour, which says nothing about the offset it DENOTES, so its bound folds in `bounds.go`'s `sectionDisplacementArea(delta, walks, perimeterUpper)` — `delta` the chamfered band's own contour displacement (`capBandResult.delta`), `walks` the loop's own recorded segment count, `perimeterUpper` read off the SAME band's built cap-level boundary (`capContourPerimeterUpper`, summing `capBandResult.capCo`'s own edge lengths and bounds) rather than re-walked, so the two readings of one contour never disagree; an unchamfered loop's zero `capBandResult` leaves the term at its own zero through `sectionDisplacementArea`'s `delta<=0` guard. Volume is the divergence theorem in the payload's own plane-local `(u, v, z)` coordinates, per loop (holes negative, mirroring the straight-prism reduction) and per band: each band is closed off by two flat "disk" areas (the offset loop's own enclosed area at the cap level, the original loop's at the side level) and its patches' own raw flux (`patchRawFlux` — the tetrahedron identity for a `Plane` patch, taken EXACTLY over `big.Rat` and rounded once (`exactPlanePatchFlux`; the float triple product survives only as the fallback for a patch whose coordinates do not lift), a closed-form polynomial-plus-trig integral IN FLOATS for a `Cone` one, each returned WITH its own bound), all relative to the plane-local origin, since a genuinely closed sub-solid's flux integral is reference-point independent. The two disks are ABSOLUTE areas, so the sub-solid they close off is the loop's region read POSITIVELY ORIENTED whichever way the loop was recorded, while every patch is built from the loop's OWN walk — which is what makes the flux sign two independent corrections, each confirmed empirically (never hand-trusted) against a reference derived from the section alone. `-matSign` covers the AXIAL half, because the topology's fixed vertex order is "outward" for one axial ordering of (sideZ, capZ) and its mirror for the other (`TestCapBlendStartCapVolumeMatchesEndCap`). `orient` — the sign of the loop's own signed area (`loopSignedAreaBudget`) — covers the IN-PLANE half, rotating a CLOCKWISE loop's patches back onto the disks' orientation; it applies to EVERY patch of that loop, since a hole's straight walls and its corners' apex cones both face into the band (`TestCapBlendPolygonalHoleChamferVolume`). Between them, `patchRawFlux` puts each patch in its OWN walk's sense: a Cone patch's `th0`/`th1` (and, for a regular wall, its second window `capTh0`/`capTh1`) are normalized to increasing at construction (`capblend_geom.go`) because the DX7 survey reads `th0`/`th1` as an increasing window, and the sense that normalization drops is kept in `sweepCCW`, whose false value (a hole's whole circle, a concave arc, and every reflex corner's clockwise apex connector) negates the flux. A whole-turn Cone is the one patch whose flux does not depend on its walk at all — its trig endpoint terms cancel — so the `sweepCCW` negation and `orient` cancel each other there exactly, which is why a circular hole reads correctly under any of these spellings and cannot witness this sign (`TestCapBlendHoleLoopChamferVolume` asserts it, `TestCapBlendReflexCornerBuilds` pins the L section's own eroded-area integral). The volume bound is composed TERM BY TERM and is never read off a summed result, because every one of these flux terms cancels: the two closing disks each carry the whole prism's flux and differ by the band's, smaller by the ratio of the sweep height to the setback, so a budget scaled by the band under-counts the rounding of the terms it came from by exactly that ratio (`TestCapBandVolumeBoundTracksTheFluxNotTheCancelledBand` measures the two readings of ONE band that a cancelled-result budget lets disagree). `capBandVolume` therefore returns a `boundedScalar`, charging each disk's own area bound at the level it sits at, the rounding of the trimmed level `sideZ` (which multiplies a whole section area, and which `evalCapBlendContext` charges identically for the slab's own `zHi`/`zLo`), each patch's own `patchRawFlux` bound, and every float multiplication and addition through `boundedMul`/`boundedAdd`, whose committed error the `big.Rat` round-error helpers take EXACTLY rather than estimate — so the composition stands whatever a platform's compiler fuses. `capBandVolume` also takes the band's own contour displacement (`delta`, from `capBandResult`) and, ONCE after the flux sum — never inside the disk area's own bound or inside any one `patchRawFlux` term, both of which already read the SAME displaced cap-level coordinates and would double the charge — adds `bounds.go`'s `sweptVolumeAllow(delta, areaUpper)`, `areaUpper` the band's own total patch area plus the cap-level disk area it closes on: the volume the cap contour's own displacement can move across the surface it acts on. A regular wall's Cone patch is the STRAIGHT-SLANT RULED PATCH between its two windows (docs/modify-reach-design.md §8.3) — a `Plane`-style rotationally-symmetric cone sector only where the two coincide (a tangent join, or the degenerate apex/whole-circle cases, where `capTh0`/`capTh1` are literally `th0`/`th1`) — and `patchRawFlux` integrates the closed form the ruled parametrization gives: an ORIGIN term (one `cU`/`cV`-eccentric sub-term per directrix) and a LOCAL term (a pure polynomial piece plus one CROSS term whose own trig factor, `ruledAngleCos`, is the product-to-sum reduction of `integral[0,1] cos(thetaS(u)-thetaC(u))du`). Both `origin` and `cross*ruledAngleCos` pass through `math.Sincos`/`sinc` and take the magnitude envelope `moments.go`'s `analyticRoundBound` doc reserves for a libm result rather than that helper's roundoff budget: `|sin|`, `|cos|` and `|ruledAngleCos|` never exceed 1 for any real argument, so neither sub-term's TRUE magnitude can exceed its own structural envelope. `poly`/`cross` are themselves regrouped about the band's own z origin (`z0`, the side level) rather than left as functions of the two absolute levels `z0` AND `z1` independently — the same split the `cU`/`cV` origin term already makes for the in-plane eccentricity, `z1 = z0 + H` splitting each into a piece proportional to `z0` and a piece proportional to `H` alone — so `origin`'s own envelope is computed from `|R0|`/`|R1|`/`|H|`/`|cU|`/`|cV|` alone and `cross`'s from those plus `|z0|` scaled by the WINDOW SKEW `|dS-dC|` (exactly zero at a tangent join or either degenerate patch) rather than the unconstrained `|z0·dS|`/`|z1·dC|` a naive envelope on the un-regrouped form would carry — never read off the computed value. A patch whose window really is the full period is the exception and is marked structurally (`capPatchGeom.wholeTurn`, set where `capblend_geom.go` builds the one cornerless closed circle or a reflex corner's degenerate apex, never from a comparison of `th0` and `th1` — `fl(2π)` is not `2π`): `thetaS(u) == thetaC(u)` identically there, so `ruledAngleCos`'s TRUE value is exactly 1 (read directly, no Sincos/sinc call at all) and `origin`'s TRUE value is exactly zero (a full period integrates both `cos` and `sin` to zero), so the held `origin` is its own whole error and `cross` folds into the ordinary polynomial rounding budget alongside `poly`. This closed form REDUCES ALGEBRAICALLY to the pre-fix single-window formula wherever `capTh0 = th0` and `capTh1 = th1` (verified both symbolically and by exhaustive numeric comparison, never merely asserted), which is what leaves every already-shipped whole-circle/apex/tangent-join reading (`TestCapBlendConePatchKeepsTaperAtHugeRadius`, `TestCapBlendVolumeBoundEnclosesExactVolume`, `TestCapBlendTangentJunctionVolumeUnaffected`) unchanged. Where the two windows genuinely differ (a non-tangential miter corner), this closed form is exact for the STRAIGHT-RULED patch the topology actually builds, but that built patch is not the DENOTED chamfer's true (curved) miter locus, and the gap between the two (`docs/modify-reach-design.md`'s own quarter-disk reproduction puts it near 2.9 mm³ against a ~407 mm³ correction, and it widens further as the setback approaches the section's own inradius) has its own dedicated proven term, `bounds.go`'s `chordLocusVolumeAllow`: erosion by an increasing offset is monotone (a point surviving the deepest cut survived every shallower one, and every survivor of any cut belongs to the wall's own un-eroded extent), so the true solid's flux is sandwiched between the ordinary rotationally-symmetric cone-sector flux this same formula gives the wide (side) window and the narrow (cap) one alone, and the built patch's own point-for-point displacement from the wide cone is bounded in closed form (a convex combination of two rays stays radially inside and angularly between them) and turned into a volume bound through the same `sweptVolumeAllow` a mesh vertex displacement already uses. `chordLocusResidualAllow` gathers one patch's own inputs to it (the wide/narrow reference fluxes, from two more calls to this same function with `capTh0`/`capTh1` or `th0`/`th1` collapsed onto the other pair) and is zero wherever the two windows already coincide, unchanged from the already-shipped tangent-junction/apex/whole-turn readings; `TestCapBlendErosionFamilyVolumeBoundEncloses` judges the composed bound against the erosion-family reference, including the setback-limit family this term exists for. `Exact` is reached by a proven-zero band (no chamfered loop) AND by an all-`Plane` cap-loop band whose exact rational flux is a float64, which is §8.4's own "report each exactly representable result as `Exact`" — the `Plane` arm's bound is the committed `rationalFloatError(exact, held)` and not an envelope, since an envelope over an exact value is still positive and publishes `Approximate`. A band holding one `Cone` patch stays `Approximate` however exactly its `Plane` patches integrate: its flux passes through `math.Sincos`, which no rational carries. Area sums the built faces' own (already-bounded) areas plus each patch's closed-form area (`patchAreaOf`, a Cone's slant length bounded like `lineWalkBounds` bounds a line, read against the CAP-level (trimmed) sweep `capTh1 - capTh0` rather than the wall's own `th1 - th0` — the actual held chord's own angle — with `windowSkew` widening the envelope by what the OTHER (side) window would have given wherever the two genuinely differ, zero at a tangent join or either degenerate patch; a Plane patch's two cross-product triangles are a float evaluation through r3's nested-`Hypot` `Len`, so their bound is `bounds.go`'s `sumSlop` PLUS `crossProductUpper`'s own envelope and is never zero — a zero there would publish an `Exact` the arithmetic has not earned, on the patch `Face` and in this sum alike, and `sumSlop` alone charges each triangle a few ulps of the area its cross product already cancelled DOWN to: a band patch is a thin ruled quad, so the wall direction crossed with the offset displacement is smaller than either product by the ratio of the wall's length to the setback, the same cancellation the volume's flux terms carry). `Bounds` (`capBlendBoundsContext`) is read along each world axis from the payload's OWN patch extrema — `capblend.go`'s `extentBoundedAlong`, DX5 — exactly as `prismBoundsContext` reads a prism's, so every face of the box is a value the body attains (§8.4: bounds from patch extrema, never a loose box). Its `Bound` is that reading's own and never a fixed zero: an extreme held by a RECORDED coordinate really has none, which is the ordinary case on an axis-aligned plate, while one held by the COMPUTED cap contour is known only to `capblend_contour.go`'s displacement, and publishing that as an `Exact` position asserts an accuracy the offset solve never had. Widening the receiver's box outward by `d` instead would put `Min` `d` millimetres from the true extreme while `Bound` — the ABSOLUTE error on `Min`/`Max` (`measurement.go`) — still read zero, an overstatement `verify.go`'s bound-only gate could never surface. Centroid then uses the SAME geometric safety-net bound `prismCentroidGeometryBound` already establishes elsewhere: the true centroid lies within that box, so `\|estimate-true\|` is bounded by the box's own reach from the estimate regardless of the estimate's own precision — sound, not razor-tight. That reach is maximized over ALL EIGHT corners, which is the proof and not a flourish: `p -> \|p-estimate\|` is convex, so its maximum over the box is attained at a corner, and reading only `Min` and `Max` leaves six unexamined — on a box far wider than it is tall the farthest corner is among exactly those six, and the published bound then encloses nothing. The box's own `Bound` is added on top, since the box that provably contains the body is the reported one widened by it. Every patch's outward `Surface` orientation (the `.reversed` bit `Face.NormalAt`/DX7 read) is set by `capblend_geom.go`'s `fixPatchOrientation`, never hand-trusted: it compares the patch's own built `NormalAt` against a reference direction and reverses only on disagreement, and each reference is itself checked against an independent closed form (`TestCapBlendPlanePatchNormalOutwardBothCaps`, `TestCapBlendConePatchNormalOutwardBothCaps`, `TestCapBlendReflexApexNormalOutward`). Tessellation (DX3/DX4) is not implemented — `Body.Tessellate` refuses `capBlendPayload` through `tessellateContext`'s existing unknown-payload `ErrUnsupported`. Clearance (DX6) is likewise not wired into `clearance_geom.go`'s kernel — `capBlendPayload` reads `Suspect` there except where the body-box test alone proves a pair disjoint (`Bounds()`/`extentAlong` still work), mirroring `cupPayload`'s own pre-migration D6 staging. |
| `capblend_survey.go` | The cap-blend payload's DX7/DX8 surveys. `capBlendUndercuts` (DX7) answers the ordinary (unchanged) side walls and caps with the SAME per-role `wallNormalRange` read `prismUndercuts` runs over the receiver's own recorded profile, and answers each new patch from its OWN built `Face.NormalAt`: `capPatchNormalRange` recovers a Cone's exact `A*cos(phi)+B*sin(phi)+C` normal-component form from three real `NormalAt` evaluations (never a hand-derived formula) in the azimuth `phi = theta − th0` measured from the window's own start, and reads it with the existing `trigRange` over `[0, th1−th0]` — the window in THAT SAME azimuth, since reading the recovered local coefficients over the global `[th0, th1]` scans the wrong arc of the circle wherever `th0` is not zero and reports a range the patch never takes. It walks the payload's patch list in Table BX row BX3's own deterministic patch order, so `Report.Bodies[i].Undercuts` — public output a caller may diff or golden-test — reports the same faces in the same sequence on every call. `capBlendMinRadius` (DX8) is `prismMinRadius` on the receiver's own untouched profile: this slice's patches are `Plane`/`Cone` only, and neither ever tightens the answer beyond what the receiver's own unchanged run of the same wall already gives (a `Plane` has zero curvature everywhere; a regular `Cone` patch's tightest azimuthal radius, `R/cos(halfAngle)`, is `>=` the unchanged wall's own radius, which SX7's band-reach gate guarantees always survives as a nonzero run; an apex-cone patch's radius reaches zero only at its own boundary VERTEX — a sharp corner/edge feature this survey's own convention already excludes everywhere else) — so the correct answer is exactly "no new concave principal radius," proven in code rather than staged `Suspect`. |
| `verify.go` | `Document.Verify` (evaluator §10/§11, verification design): `Report`/`BodyReport`/`Status` (zero `Unverified`, explicitly initialized real reports, worst-wins precedence) + `Trustworthy()`, the branchable `Report.Diagnostics []Diagnostic` (verification §1.1: `Diagnostic`/`DiagnosticCode`/`ReadingKind`/`DiagnosticPair` — on a returned report, one entry per non-Sound reason, empty EXACTLY when `Sound`, worst diagnostic status == `Report.Status`; `DiagnosticCode`/`ReadingKind` are named-text enums with pinned `String()` tokens; exactly one `Observed`/`ObservedVec`/`ObservedBox` keyed off `Reading`, `Required` the judged threshold), `Interference`/`Clearance` row types, the `VerifyOption` set (every parameter validated at the call, never deferred into the report), the structural boundary audit (validity by construction), the Exact-passes tolerance gate, the pair partition — box-disjointness proofs first, then the always-on clearance-kernel proof (`clearance.go`) for every pair boxes cannot decide; an undecided pair reads `Suspect`, never a fabricated row — the overlapping side of that partition minting an `Interference` row from `interference.go`'s read-only overlap-volume proof — and the wiring of the increment-5 analytic surveys (`survey.go`): asked on a proven solid, each survey answers outright, the interval rule decides `MinWallThickness` against the tool (proven thin → `Violating` at any coarseness, met → the gate judges the bound, straddle → `Suspect`), a non-empty `Undercuts` is `Violating`, and `MinRadius` is a measurement, never a verdict. `WithClearances` emits a `Clearance` row per proven-disjoint pair — `Gap` = the kernel's interval midpoint, `Bound` its half width, `Exact` iff the interval is a point — gated by `gapWithinTolerance` (clearance §7: `Ref = max(\|Value\|, ε·D)` with `D` the PAIR's kernel-read diameter; a gap beyond tolerance makes the report `Suspect` directly); a gap the kernel cannot prove yields no row and reads `Suspect` only when asked. `bodyGateDiameter` forms every body's own tolerance-gate diameter (verification §3) and never falls back to a box diagonal, document scale or zero: a `facetedPayload` reads its own cached value, and a `prismPayload`/`revolvePayload` reads the clearance kernel's exact carrier model (`newBodyGeomBudget`) — except a `prismPayload` whose own `sectionDelta` is nonzero (`docs/prism-boolean-design.md` §7's re-expressed section, e.g. the analytic `Union` of a placed prism pair): that model refuses it, `envelopePrismFor` has no arm for a bare `prismPayload` either, and `bodyGateDiameter` returns no diameter at all — every `DiagMeasurementBeyondTolerance` such a body's area/bounds/volume/centroid readings raise then carries a nil `Required`, the one documented reference-less `Suspect` this design admits (`verify_diagnostics_test.go` pins it). `envelopeGateDiameter`/`envelopePrismFor` cover the two payloads the exact model does not reach a different way each: `capBlendPayload` reads the receiver's own unrewritten section on its unchanged interval, since a cap-loop chamfer only ever cuts within it; `cupPayload` reads `pl.outer`, the cup's own outer region — the receiver's unmodified section for an INWARD shell, but the wider OFFSET region for an OUTWARD one, since an outward shell adds material and `cupPayloadFor` always assigns the wider profile to `outer`. Either arm's envelope, AS A SHAPE, still contains the whole body (the cavity never reaches past `pl.outer`'s own full-height prism, the same containment `cupPayload.extentAlong` relies on), so it can only overstate the true diameter, never understate it. The reading taken off that envelope goes through the same sparse three-angle witness a shipped `prismPayload`'s own circular-wall and cap-arc sampling already reads its diameter through (`addPrismFaces`'s mid-angle/`th0`, `region2.samples`'s per-arc `th0`/mid-angle) — exact exactly when the body's farthest pair lands on one of those three sampled angles, which no bound on a wall's sweep alone guarantees: an all-line section, a full circle, and the arc-plus-chord family at or below 180° all land there, but an outward cup's own four 90° corner arcs already understate this fallback's own reading (64.922642 against that body's true diameter 65, a ratio of 1.0012 — its bounding-box diagonal is 68.738635, which the rounded corners keep it well inside of), and a bare arc-plus-chord section still peaks at 2/√3, about 15.5%, at 240° (verification design §3). Understating only tightens the gate and can turn a passing reading into a false `Suspect`, never a false `Sound`. |
| `clearance.go` | The clearance pair kernel (docs/clearance-design.md §1/§2/§5/§6/§7): `clearancePair` proves one pair's four-way relation and gap interval. Order is load-bearing: the coplanar `Plane`×`Plane` contact certificate first (strictly opposing outward normals + coplanar carriers + proven positive-area trim overlap + each body's exact `extentAlong` wholly on its own side — a separating plane, so the whole contact set is certified and the gap is a measured `Exact` zero), then tier enumeration; an admitted transversal crossing sets `overlap`, which is read before `unsure`, while unsupported contact remains undecided. The interval is `[min lo, min hi]` over contributions with §5 pruning (a `lo` at/above the best `hi` cannot hold the minimum); **exactness is the interval's, not the winner's** — `Exact` iff a closed-form winner has every rival's `lo` at or above it. `lo` must strictly clear the tolerance floor, then §2 nesting runs (skipped when boxes already exclude it): one witness per SHELL — void shells included — is cast against every face of the other body, since disjoint boundaries make membership constant on each connected component of one body MINUS the other body's shells, never across the lump as a whole. Strict single-lump analytic containment therefore needs BOTH directions — every witness of the contained body inside the other, AND every witness of the other, its voids among them, outside the contained one, which is what proves the outer boundary misses the inner body entirely — and returns `pairOverlapping` with the contained body so `interference.go` can reuse its bounded volume; an outer void cutting through the inner body fails the certificate and leaves the pair to read-only intersection. Faceted and multi-lump bodies bypass that analytic model entirely and use read-only mesh intersection for any interference measurement. The pair-`D` reading uses analytic support points; interference extends it with every held faceted-payload vertex, including non-topological tessellation vertices. All long scans propagate cancellation. |
| `clearance_degen.go` | **The degeneracy oracle — the one gate between a closed form and a certificate.** Every cell that would emit a constant/`Exact` candidate, take the nested branch or grant a crossing exclusion asks here first, and the answer is THREE-valued (`degYes`/`degNo`/`degUnknown`, never a bool): `degYes` only on EXACT arithmetic over the payload's own floats (`ratV3` cross/dot products over `math/big.Rat` — clearance_poly.go's take-the-floats-exactly discipline), `degNo` only on a residual clearly above the kernel's noise, `degUnknown` in the band between. A tolerance may therefore route a question to `unsure`; it may NEVER route one to a certificate — deciding parallelism/coaxiality/on-axis-ness with `clrAngTol` mints an `Exact` the true answer undercuts by up to the tolerance. Members: `parallel`/`parallelSeg`/`parallelSegs`, `perpendicularSeg`, `onAxis`, `coincident`, `coaxial`, `ringFamily` (§4's coincident-spine families), and `spineSup` — the BOUNDEDNESS gate of §4's nested branch: `d_sup` is a SUPREMUM over the inner spine, so a cylinder's infinite LINE spine has `d_sup = ∞` against any bounded partner and is finite only against an exactly parallel line; a point spine's supremum is its own distance, a circle spine's needs exact coaxiality. `certifiedContainment` reads it, which is what keeps a rod piercing a ball from certifying as nested. The accepted cost (clearance §4): a rigid motion landing a meant-to-be-coaxial pair a few ulps off true reads `Suspect`. |
| `clearance_cells.go` | The §3 candidate sink + the §4 face-interior table. `enumerate` owns one shared `workBudget` for every outer tier cell and passes it into both vertex-tier directions, so nested work cannot restart the cancellation interval. `cellSink.candidate` folds admission: rejected feet are discarded (a lower tier holds the minimum), a straddle keeps only its lower bound (never counted toward exactness), a near-zero value not cleanly rejected is `unsure` — a possible contact only §6 certificates may bless. The spine-offset reduction (`offsetPair`): spine-pair criticals (point/line/circle pairwise — CF where the oracle proves the configuration degenerate, else the P4/P8 brackets, else the coarse enclosure), the four offset combinations per critical, coincident-spine ring families for a PROVEN `ringFamily` only (`emitRingCombos` — the coaxial peg-in-hole reading; its azimuths are a deterministic SAMPLE, so an unadmitted near pairing keeps its proven lower bound rather than dropping a family that may hold the minimum), then crossing exclusion by the STRICT exterior branch, `certifiedContainment` (§4's list, through `spineSup`), or face-box separation; else `unsure`. The plane row: coplanar pairs reaching the cells are uncertified (`unsure` unless provenly apart in-plane), parallel plateaus admitted by projection overlap (`coplanarRelation`, probe-sufficient/exclusion-sound), crossing carriers excluded through trim-interval SUPERSETS on the shared intersection line (exclude-only, never bless), axis-parallel cylinder rulings/sphere and torus plane extremes CF with per-trim admission and exact crossing exclusion (`planeCrossesRevolved`'s axial range + the crossing circle of an EXACTLY perpendicular axis — a tilted axis crosses in an ellipse and earns no exclusion). `planeCone` emits no face-interior plateau: `|n·axis| = sin α` is a transcendental identity no exact test on the stored floats can certify, and the cell does not need it — the plane distance is affine along every ruling, so the minimum always migrates to the latitude edges and the synthesized apex, which hold it exactly. Cone-involved and spindle-torus cells are PR 2's BB path: coarse `[face-box distance, closest witness pair]` only. |
| `clearance_tiers.go` | The curve and vertex tiers of §3 over §4's curve-tier table: face×edge (`feCell`: line/circle × plane CF with in-plane contact handling, line/circle × offset faces via spine criticals ± the offset with `feCrossingExcluded`'s superset range; ×cone coarse), edge×edge (`eeCell`: line×line CF incl. the parallel family, line×circle P4 with the axis-parallel CF special case, circle×circle P8 with the coaxial CF special case), and the vertex tiers (`vertexTier`: point×surface and point×curve, all CF) — vertices include the synthesized cone apexes and spindle axis-collapse points (surface singular points where no interior first-order condition holds). `vertexTier` continues the enumerator's shared budget through each inner face and edge, and planar `vertexFace` continues it through trim classification. Every constant-distance family here (a line parallel to a plane or to an axis, a circle whose plane parallels the face's, a coaxial cap edge, a parallel segment pair) is emitted only on the oracle's `degYes`: these objectives are affine along the carrier, so a `degNo` migrates the minimum to the trim boundary where the endpoint/vertex tiers hold it exactly, and a `degUnknown` owes an honest lower bound (`loOnly`, off the untrimmed carrier distance), a coarse enclosure or `unsure` — never a plateau. |
| `clearance_geom.go` | The kernel's boundary model (§2/§3): `bodyGeom` reads trimmed carrier faces off the evaluator's own payload (the same source the surveys read) — `cFace` with closed-form trims (angular×axial/meridian windows on revolution faces — a window is `full` only when it really closes on itself, since `full` is an ADMISSION and a tolerance rounding a window up to it admits carrier points in the sliver the trim is missing; `region2` line/arc-loop parity regions on planar ones, with strict/ambiguous/outside three-way `classify`, line-interval and conservative line-interval-SUPERSET queries, segment tests; the budget-aware classifier charges every boundary-distance element, retry direction and winding element) — plus the topology's exact edges/vertices, per-face containing boxes and on-face witness points. Also the §2 nesting casts (`pointInBody`): a fixed deterministic direction ladder, crossings certified transversal and trim-interior per variant — plane/cylinder/cone/sphere closed form, the torus quartic by Sturm isolation (`torusCrossings`: a non-square-free quartic is a tangency, ambiguous); a carrier through the ray start counts only if the start provably misses the trim (witnesses on shared construction planes). |
| `clearance_poly.go` | The certified-bracket machinery of §4/§5: `ratPoly` over `math/big.Rat` (Sturm chains, square-free reduction, Cauchy-bounded root isolation, deterministic bisection refinement with a fixed depth budget — on exhaustion the honest wide interval stands), the `csPoly` trig-polynomial form `A(c)+s·B(c)` with exact d/dθ, quarter-turn chart shift and half-angle conversion (two charts cover the circle), and the cell builders — `lineCircleBrackets` (P4) and `circleCircleBrackets` (P8, the squared stationarity `s·(w′)² = r²·(s′)²`, ρ=0-kink guarded by callers) — each returning enclosures of every critical VALUE via a proven Lipschitz bound (distance to a fixed set is 1-Lipschitz, parameterization speed ≤ r). Coefficients are the evaluator's own floats taken exactly: a root count is a proof only when the isolation certifies it. |
| `survey.go` | The increment-5 analytic surveys (verification §6, evaluator §10): wall/undercut/min-radius answered outright on the evaluator's own payloads. The wall reading reduces exactly to 2D — a prism's spanning balls are its profile's spanning disks with the height as the vertical fit (the parallel caps span when a disk of half the height fits the section), a revolve's are its meridian section's (mirrored across the axis for a full turn, since a ball's contacts with surfaces of revolution lie in the meridian plane through its center; wedge-constrained by the caps for a partial one, whose on-axis cap edge has the sweep angle itself as its dihedral); a junction dihedral within the allowance pinches to a genuine `Exact` 0 (knife edges, in-allowance tapers). A cup's `cupWall` reconstructs and audits the exact offset relation before returning the same pinch zero or the Exact floor/skin thickness `t`; an inconsistent payload stays undecided. Undercuts are per-face normal-range membership over exact per-variant ranges: a face with a provenly opposing point is listed, exactly perpendicular clears — **and so does a face exactly antiparallel everywhere** (a prism's flat base separates under the pull rather than hooking it; the one reading that makes the proven empty all-clear §1 promises attainable on a closed solid — a deliberate, documented narrowing of §6's pointwise `n·p < 0`); walks map to `*Face`s by feature role. `MinRadius` is the tightest concave principal radius (meridian arcs walked against the material, plus the parallel circle's ρ/\|n_ρ\| where the meridian normal points at the axis — hole walls, a donut's waist). A CUP answers the undercut survey (`cupUndercuts`, modify §12 D2 — outer walls over EVERY loop `side(i,j)`, cavity walls over every reversed loop `shellSide(i,j)`, and the caps: the kept cap, the pocket floor and one rim per loop, whose ±N sense follows which side the open end lies on) and `MinRadius` (`cupMinRadius`, D3 — the same walk over every loop of region O and every loop of the cavity read reversed, so a tunnel's and pocket wall's concave radii are read while a convex outer round or a post's outer cylinder rightly is not), for any `k ≥ 0`: a post wall's undercut is caught and a tunnel's radius read, never dropped. `nil` readings are proven absences; a payload the surveys cannot decide leaves the asked question undecided → `Suspect`, never a silent pass. |
| `survey2d.go` | The 2D kernel behind the wall survey. `walkElem` (in `survey.go`, aliased as `elemOf` in `fillet_audit.go`) is the ONE conversion from a `segmentWalk` into this vocabulary, shared by the wall survey, the modify section audit and the clearance kernel's planar trims — its switch is total over `walkKind`, so a free-form walk answers `false` rather than collapsing into the straight line between its endpoints. The kernel itself: streams a closed-form candidate set of critical inscribed disks — Apollonius triples (material-side-pinned tangency equations; quadratic pairs linearize by subtraction, parallel linear pairs pin the radius), antipodal pair criticals, angle-limit disks at exactly the allowance boundary (the closed-under-limits families of §6; within is inclusive, so a wall drafted at exactly α spans), concentric whole-arc contacts, and wedge-tangent minima — directly into whole-boundary validation: distance clearance, ray-parity containment (ambiguous hits retry across 16 directions), and contact-direction spanning over `dirArc` sets. Generation + validation can share a caller-supplied hard work counter; inward shell admission uses modify §8's fixed limit. The set is complete for the attained infimum/supremum over line/arc boundaries, so the minimum spanning diameter and the 2D inradius are exact; a containment the retries cannot decide fails the survey to `Suspect`. |
| `tessellate.go` | Current `Mesh` + `Body.Tessellate(tol)` implementation: an OUTPUT, never the representation. Prism, cup (`tessellateCup`, modify §12 D4 — any `k ≥ 0`) and boolean-faceted payloads build; revolve remains staged (`ErrUnsupported`). Boundary curves are chorded once and shared by incident faces; facets keep their source `*Face`; `Mesh.Bound` is the proven boundary bound. `docs/tessellation-design.md` is the target contract; its per-source and occupied-volume proof record has not landed. |
| `triangulate.go` | The cap triangulator behind `Tessellate`: polygon-with-holes → CCW triangles by hole bridging (Eberly max-u visibility — `bridgeHole` bridges to the intersected edge's max-u endpoint, never a nearer vertex short-circuit, keeping the bridge off a hole edge the +u ray runs collinear with) + reflex-blocked ear clipping — correct for non-convex outlines with holes, deterministic. `earClip` collapses a zero-area corner only when it is a bridge STUB — a zero-width there-and-back channel recognised by an index coincidence among the corner's three vertices (`isBridgeStub`: the spike apex shares its anchor index on both sides, the residual doubled anchor shares an adjacent index) — so collapsing drops only a bridge edge and its exact reverse. A collinear corner whose three vertices are DISTINCT indices is a GENUINE boundary corner (its two edges real, possibly one from each of two holes sharing a horizontal v-line) and is deferred, not dropped; so a bridge-collinear band — a rounded outer loop whose corner tangent points land on a sharp inner loop's edge lines (the rectangular post, the outward box cup), OR two holes in distinct v-bands sharing a v-line where the bridge runs collinear through another hole's edge — triangulates watertight rather than cracking. A stalled clip, and a bridge that pinches against the boundary, are refusals of the CHORDING rather than the operand — each carries the private expected-coarse marker `tessellate.go` owns, so read-only interference takes it as an undecided pair while public `Tessellate` still sees `ErrDegenerate` through `Unwrap`; a hole genuinely outside its outline stays an untyped `ErrDegenerate`. Never a wrong mesh. |
| `export.go` | `Body.STL`/`Body.OBJ` (core §11): deterministic writers over `Tessellate` — ASCII STL (chosen for diffable, byte-stable output), OBJ vertex table + 1-based faces. `WithChordTolerance` is an `STLOBJOption` (jwx combined-interface pattern) both accept; without it analytic payloads use 1/1000 of the body's bounding-box diagonal, while faceted payloads use the larger of that value and their retained `meshBound`. |
| `bounds.go` | The single owner of every proven error bound a faceted (boolean) measurement reports — ONE helper per error mechanism, and NO measurement site computes a bound inline. `radius3D` (a per-coordinate bound read as a 3D distance is ×√3, up-rounded); `sumSlop` (a PROVEN bound for the NAIVE float sum that produced the value — never zero for a nonzero FINITE float-computed quantity, which is what keeps `exactnessOf` from claiming an Exact a float sum of square roots does not have; a saturated non-finite scale answers 0 and the caller owes its own bound for that case); `chainLengthBound` (a rim of nSegs chords, BOTH endpoints of each displaced: 2·nSegs·δ + the sum's own slop); `rigidRoundAllow` (ulp at the INPUT and translation magnitudes, ×√3); `sweptVolumeAllow` (the volume a vertex displacement moves — δ × the area of the surface the displacement acted ON, which is NEVER the area of the mesh that survived it) over `perturbedAreaUpper` (the area of a mesh whose vertices may each sit δ from the held ones, and of every mesh on the way — the only term that speaks for a facet the weld flattened to zero area); `sectionDisplacementArea` and `sectionDisplacementLength` (the same identity one dimension down, `docs/prism-boolean-design.md` §7: a recorded 2D section whose every coordinate sits within δ of the section it DENOTES differs from that section by at most the δ-tube about its own boundary, `2·δ·p + n·π·δ²`, and its perimeter differs by at most `12·π·δ` per walk — the figure that covers a circular walk's radius AND its swept angle moving, where a straight walk's two moved ends alone give `2·δ`; a SET-displacement bound, so it stands even where the displacement changes which regions a construction merged, which no coordinate-arithmetic term reaches); `bandPatchAreaAllow` (the cap-loop chamfer's own per-patch reading, `docs/modify-reach-design.md` §8.4: ONE ruled quad between a side-level chord — whose own rounding this bound does not charge — and a cap-level chord displaced by δ changes area by at most `sectionDisplacementLength(δ, 1)` — the chord's own possible length change — times the patch's held slant distance, plus the held chord length times δ — the slant's own possible change, since only its cap-level endpoint moves); `chordLocusVolumeAllow` (a cap-loop chamfer's regular-wall Cone patch, at a non-tangential corner: erosion monotonicity sandwiches the true miter-locus flux between the ordinary shared-window cone-sector flux read at the wide (side) and narrow (cap) windows, and the built straight-ruled patch's own point-for-point displacement from the wide cone — bounded in closed form from the two windows' skew and the two directrix radii — feeds `sweptVolumeAllow`, zero wherever the two windows coincide); and `rimDelta` — the trim amplification: a vertex the boolean CREATES is the crossing of two chord PLANES, so it is displaced by (δA+δB)/sin θ, unbounded as the operands approach tangency, and when the inflated bound reaches the pair's diameter it has stopped bounding anything and the operation is `ErrUnsupported`. |
| `budget.go` | `workBudget` — the ONE shared bounded work counter every read-only scan polls through (docs/interference-design.md §7.2), and `workPollInterval`, the 256 it polls at. Leaf exact predicates stay context-free; their callers `step()` this counter, which returns `ctx.Err()` on the interval, and call `err()` at a phase boundary, which polls unconditionally. The counter is SHARED across a phase's nested loops on purpose: a nest whose loops each counted to the interval alone would let the innermost scan run a full interval's work for every step of the outermost. It holds closures, not a context, so no long-lived geometry state contains one. |
| `interference.go` | The pairwise overlap measurement behind `Verify`: strict containment of shipped single-lump analytic payloads reuses the contained body's whole `Measurement`; multi-lump faceted bodies bypass analytic containment and use read-only intersection. Exact equality of analytic represented-set fields reuses the first body's volume; derived payload metadata is excluded, and harmless alternate record spellings remain staged. Every other supported pair runs read-only `OpIntersect` and emits only when `Value-Bound > 0`. The pair-local tolerance gate uses the pair diameter and summed operand areas. Expected empty/coarse outcomes stay undecided; payload staging, contact policy, and later pipeline reach stay separate for cause-specific diagnostics; internal failures return. |
| `boolean.go` | `Union`/`Cut`/`Intersect` (core §8, evaluator §9) over the shared read-only `evaluateBoolean(ctx, …)` geometry path used by interference verification: package-level, no target-out, operands retired and the result registered only by the public wrapper, through the operands' own document; gates (nil/self `ErrDegenerate`, `ErrForeignBody`, `ErrRetiredBody`); the evaluator-internal chord tolerance (pair bounds-box diagonal × 2e-5 — a documented default, never caller-set; its whole effect surfaces as the result's proven `Bound`); the TANGENCY GATE (`refuseUndecidableProximity`, `docs/evaluator-design.md` §9 + `docs/tessellation-design.md` §11 step 4) admits a held-facet meet only when the facet sets interpenetrate DEEPER than δA+δB — a proven true crossing via `provenDepthExceeds` (a certified interior witness: a held-facet sample point proven strictly inside the other solid by exact ray parity, with a certified lower-bound distance to that boundary exceeding δA+δB, since the chord error alone cannot open that depth) — and refuses every other positive-bound pair within that distance as undecidable — a touch, a shallow meet ≤ δA+δB, or a non-meeting near-miss (reject-only, so it may over-refuse a genuine pass that close); analytic δ is current chording displacement, while the shipped faceted path returns zero from `faceChordDelta` because per-face certificate/`Delta` propagation has not landed — neither `boundaryCert.Delta` nor the per-facet `sourceBound` that would ride beside it exists in the Go, and the refusal a faceted operand owes once they do is specified in payload verification §5.3/§13, never implemented here; and the bound composition — volume by the symmetric-difference identity (Σ per-operand δ·held-area + final rounding swept over the pre-round surface), boundary by the upward-rounded sum of `rimDelta` and final weld coordinate displacement, centroid by displaced mass against the pair diameter. Private typed outcomes distinguish expected empty/contact/staging/coarse-chording results from invariant failures; `asBooleanError` maps them at the public boundary to the H2 typed `BooleanError` (core §8): empty → `BooleanEmpty`, a valid-but-unclassifiable contact OR an in-pipeline reach on operands that DID tessellate (a proximity refusal, a collapsed/welded-away facet, a rimDelta trim amplification) → `BooleanUnsupportedContact` (wrapping `ErrUnsupported`, never `ErrDegenerate`), an ordinary `ErrBooleanFailed` invariant break → `BooleanEvaluatorFailure`. A capability/staging limit reached BEFORE any contact — an operand this evaluator cannot tessellate (a revolve body, or a Faceted operand coarser than the pair tolerance) — is NOT a contact refusal and passes through as a plain `ErrUnsupported`, not a `BooleanError`; a coarse-chording refusal likewise passes through as a retryable `ErrDegenerate`. The public step records `Inputs` as the two operands' `StepRef`s — `[target, tool]` for cut — and consumes both; the recipe and document stay untouched on any failure. `performBoolean` tries `prism_boolean.go`'s analytic reduction first (`tryPrismUnion`, `Union` only — PR1); a miss falls through unchanged to this tessellation path. |
| `prism_boolean.go` | `docs/prism-boolean-design.md` PR1: the analytic reduction for `Union` over co-directional coplanar straight prisms, dispatched from `performBoolean` ahead of the mesh path. `tryPrismUnion` applies the reject-only G1–G6 gate under one work budget, including the G4 segment scan. A miss stays `ok=false, err=nil`. Past the gate, `resolvePrismUnion` builds the private scene and lets `prismProfilesContext` wait for its isolated synchronous `s.Profiles()` worker before returning `ctx.Err()`, selects every returned cell, drops shared boundary edges, and chains survivors into one closed walk. A resolved candidate's `ErrUnsupported` refusal becomes `BooleanUnsupportedContact`, preserving `errors.Is(err, ErrUnsupported)`; `ErrDegenerate` and `ErrUnrecordableProfile` retain their documented sentinels. `auditPrismUnionSection` reuses `auditRewriteBudget` with an empty blend map. A resolved merge rebuilds through `evalPrism` with `sectionDelta = max(A's incoming value, B's incoming value + new re-expression allowance)`, with B's positive sum rounded outward. It is `Exact` only for a line-only section with zero resulting displacement. The result has fresh roles, including `capStart` and `capEnd`, under the Union step and does not preserve operand origins. `Cut`/`Intersect` and the general crossing sub-case remain staged. |
| `boolean_mesh.go` | The §9 mesh-boolean pipeline. ONE symmetric classifier owns every contact question (`triTriClassify`): two closed triangles are convex, so their intersection is empty / a point / a segment / a 2-D region, and the routine computes WHICH — direction-free, with endpoint-on-boundary flags decided by testing the point against EACH triangle (`onTriBoundary`), never by which crossing list it came from. These VALID-but-unclassifiable contacts refuse through `errUnclassifiableContact` (wrapping `ErrUnsupported`, `BooleanUnsupportedContact` at the public boundary, NEVER `ErrDegenerate`): a 2-D region is a coplanar face-on-face tangency; an isolated point contact no crossing chain owns is one; branching intersection curves are one. GRAZE-VS-CROSSING LEAVES THE PAIR: a segment running ALONG a facet edge is only REPORTED by the pair (`segAlongEdge`); `meshBoolean` decides it once with the adjacency in hand (`edgeCrosses`) — the edge's two adjacent facets strictly on one side of the other facet's plane is a true graze (also `errUnclassifiableContact` — `BooleanUnsupportedContact`/`ErrUnsupported`), straddling is an ordinary crossing whose segment subdivides only the facet it passes THROUGH (a segment on a facet's own boundary cuts nothing off it) and whose regions classify by exact parity (`viaParity` — the other operand's boundary there is a DIHEDRAL, and one plane of a dihedral decides nothing), with the crossed mesh edge BLOCKED so the uncut flood fill cannot merge the two facets it separates. Classification otherwise: a chain-anchored region by the exact side of its partner facet's plane; an uncut component by one exact axis-ray parity seed, flood-filled. The pass also returns the smallest crossing-angle sine it saw (`sinSquared`/`sinLowerBound`), exactly from the facet normals — what `rimDelta` divides by. Then stitching by shared exact vertices, the conforming pass (a vertex exactly interior to another facet's edge splits that facet — exact incidence, nothing moves; `conformScan` owns the pass's searchable vertex set — exact vertices, their float approximations, the coarse uniform grid over those approximations, and the one `segAdmissionRadius2` threshold every candidate is measured against, read from the mesh-wide largest `\|coordinate\|` so a single τ serves every segment — and `edgeInteriorHits` puts `boolean_exact.go`'s reject-only `segFilter` in front of the rational `onSegmentInterior3`, since a long diagonal edge's slack box covers most of the grid and nearly every vertex in it would otherwise earn a full `big.Rat` cross product; the traversal itself still walks the whole slack box, a SUPERSET of the cells the edge can touch, because a subset traversal would owe a completeness proof and measured no faster once a visited cell costs only a handful of float operations), the directed-edge closure audit (`ErrBooleanFailed` on any break), then float rounding with coincident-float welding, re-audited. A COLLAPSED OPERAND facet (a rigid placement's rounding can flatten one) is refused at `prepBoolMesh` (`ErrUnsupported`): with no plane and no interior it is invisible to every contact predicate here, so a contact made on it would be classified by nothing at all. What the WELD collapses is answered in two ways: a facet dropped from a component that SURVIVES is an edge contraction, bounded (its swept volume charged against the PRE-ROUND surface, `stitchedMesh.preArea`, and the area it takes with it, `dropArea` — the held mesh's own area is the wrong yardstick, the dropped facet is missing from it); a component EVERY facet of which collapses is welded out of existence and is refused (`refuseWeldedAwayComponent`, `ErrUnsupported`) — the lost lump is a topological loss no bound can speak for, and the closure audit misses it because the components that remain still close. |
| `boolean_cut.go` | Per-facet exact subdivision: contact segments interned into chains (endpoints on the facet's own boundary; a closed chain — the other solid punching through the interior — is opened by vertical split lines chosen through no existing vertex), convex line splits, polygon splits along chain paths, exact ear clipping that keeps every collinear vertex (what keeps a conforming subdivision conforming), and the per-region classification anchor. All rational 2D on the facet's own plane projection. |
| `boolean_exact.go` | The exact-arithmetic kernel (§9's discipline): adaptive orient3d — a float evaluation with a proven forward-error filter, falling back to `math/big.Rat` exactly at the boundary cases — plus the rational point/segment/polygon predicates and the axis-ray mesh parity test with deterministic retries (an ambiguous ray tries the next axis; all six ambiguous is a loud failure, never a guess). Also `segFilter`/`segAdmissionRadius2`, the REJECT-ONLY float pre-filter in front of `onSegmentInterior3`: it may only ever say "provably too far", and the exact predicate still decides every candidate it does not reject — a filter that could ACCEPT would be an admission gate on a residual, and one that could reject a true hit would hand back a non-conforming subdivision, which is a wrong boolean rather than a slow one. `segAdmissionRadius2` carries the derivation of τ, the admission radius: a candidate exactly on the EXACT segment is a convex combination of its endpoints, so its float rounding sits within `\|P−p\| + max(\|a−A\|, \|b−B\|)` of the FLOAT segment, and each of those three terms is one coordinate-wise round-to-nearest — bounded by `2⁻⁵³·\|fl(x)\| + 2⁻¹⁰⁷⁵` per coordinate, hence `√3·2⁻⁵³·maxAbs + √3·2⁻¹⁰⁷⁵` per point, hence `maxAbs·2⁻⁵⁰ + 2⁻¹⁰⁰⁰` for the pair with 2.3× margin over the true figure; the conforming pass's own grid slack is added on top so the filter is never tighter than the grid box preceding it. `tooFar` is the clamped point-segment projection with `segFilterErrCoef = 64·u` covering its own evaluation against the branch's magnitude (the derivation puts the worst branch at `24·u·W`), plus one `2⁻¹⁰⁰⁰` absolute floor (`segFilterFloor`) for every gradual-underflow crumb NOTHING AMPLIFIES AFTERWARDS. That bound is a RELATIVE one and holds only over normalized arithmetic, which the evaluation leaves at BOTH ends of the range. UNDERFLOW reaches the INTERIOR branch, the one place it goes while the positivity guards on `ww`/`vv` still read healthy: `c₁²` sits at the `(D·L)²` scale — the SQUARE of the scale those two sit at — so it goes subnormal at the square root of the coordinate scale that would underflow either, and the division by `V` that follows amplifies the absolute crumb it lost back up to the scale of `W`, which the final-threshold floor cannot speak for. The branch answers that twice: it forms the subtrahend as `(c₁/V)·c₁`, so the division comes FIRST and every intermediate sits at the scale of the result rather than at `(D·L)²`; and it ABSTAINS — returns false, sending the candidate to the exact predicate that decides it anyway — unless `c₁` clears `segFilterMinDot` (`2⁻⁵¹¹`, the smallest `c₁` whose square is normal) and both intermediates clear `segFilterMinNormal` (`2⁻¹⁰²²`). The rescale improves the arrangement; the ABSTAIN is the guarantee, needing no error analysis of its own because it is what the filter already does for every candidate it cannot prove far away. OVERFLOW is the same bound leaving the same range at the other end, and it is TESTED rather than argued: a saturated intermediate does not merely widen the answer, it REDIRECTS THE BRANCH — `Inf < Inf` is false, so a saturated `vv` sends a mid-segment projection down the clamp-to-B arm, which then computes a perfectly finite `\|P−B\|²` and rejects on it with no NaN anywhere. So `tooFar` tests every intermediate it forms — `vv` (hence `v`, hence `a`/`b`), `ww` (hence `w`, hence `p`), `c₁`, and `\|P−B\|²` — for finiteness before anything reads it, in the order the code forms them, and abstains on any that is not; the enumeration in the doc comment is the list of those operations, not a list of expected failures. The `vv` gate is the load-bearing one: behind it a true hit's `ww`, `c₁` and `\|P−B\|²` all sit within a rounding of `vv` itself, so the other three are reachable at most in the last ulp and are guards rather than live routes. `segAdmissionRadius2` closes the comparison from the other side, returning `+Inf` — the reject-nothing threshold — for a NaN, infinite or NEGATIVE argument (a negative slack cancels the rounding term and would leave τ² BELOW the proven requirement) and for a τ² its own square overflows; the left-hand side is always finite, so a finite value is never above `+Inf`. |
| `boolean_body.go` | `facetedPayload` + context-threaded `buildFacetedBody`: facets grouped into one Face per CONNECTED PATCH of a source face — the source's origins ride onto every patch, so `FaceCreatedBy` provenance survives the boolean, while a source the boolean cut into pieces that no longer touch (a blind trench crossing a cap) reports each piece as the separate face it now is, rather than merging them into one face whose second outer boundary would read as a HOLE in the first. Real Loop/Edge/Vertex topology chained along face boundaries (chains split where the face pair or the exact hinge convexity changes; loops walked through the facet fans, a walk starting mid-chain listing that chain ONCE; each face gets exactly one outer loop — on a planar patch DECIDED, the loop whose area vector about the patch's own outward normal is largest, since the boundary is walked material-on-the-left so holes turn negative and a serpentine slot never out-votes the boundary it is cut into by mere length; on a curved patch, where no loop is a hole in another, the longest boundary stands as bookkeeping and validity never reads it), shells/voids by exact signed volume + exact containment parity, and measurements integrated exactly (`big.Rat`) over the held float mesh, reported with the composed proven bounds — every one of them from `bounds.go`, NEVER computed inline. Long faceted placement and rebuild loops poll the caller's context. `exactnessOf`'s zero bound is a CLAIM that the value is exactly representable, so only the exact rational integrals (carrying their own explicit rounding term) can reach it: an all-planar boolean whose contacts round exactly keeps an honest Exact VOLUME, while a float-computed length or area never does. The area bounds multiply the FACE perimeter — the sum of the faces' own loop lengths, which counts every rim twice, once per face it separates and perturbs. `placed` re-evaluates under the delta motion, charging the transform's rounding at the INPUT and translation magnitudes (`rigidRoundAllow`) — where it is actually committed, not at the result's. |
| `examples/` | Executable Go examples (`Example_decad_…`, `go test`-verified `// Output:` blocks) that double as living documentation. Never `package main`. |
| `.github/workflows/` | `ci.yml` (lint → test/tidy/govulncheck), `codeql.yml`. |

**Section displacement, and who reads it.** `prismPayload.sectionDelta`
(`docs/prism-boolean-design.md` §7) is the greater of A's incoming boundary
displacement and B's incoming displacement plus its new re-expression allowance.
`evalPrism`
composes it into every measurement it publishes (see the `extrude.go` row), and
`tessellate.go` adds it to the prism mesh's own `Bound` — the vertices sit on the
RECORDED section. Every other consumer either cannot state it or has no bound to
state it in, and each therefore withholds its answer rather than measuring the
recorded section as though it were the denoted one: `survey.go`'s wall and
`MinRadius` readings answer undecided (which reads `Suspect`; the undercut
reading is a normal-direction membership and is unaffected), `ThroughAll` and
`ThroughAllSide` return `ErrUnsupported` because their recorded stop has no
bound to widen, `clearance_geom.go` builds no model for the pair, `interference.go` withholds the equal-records
set-identity certificate, `boolean.go`'s `faceChordDelta` charges it as a planar
face's own displacement so the mesh path's tangency gate cannot read the face as
held exactly, and `Fillet`/`Chamfer`/`Shell` refuse the receiver
(`requireExactSection`, `ErrUnsupported`) because a section rewrite has no proven
displacement of its own. Every one of these is a no-op at the zero every
caller-drawn payload carries.

The cap-loop chamfer's CAP CONTOUR is the same idea one construction over, and
it is a separate term with its own owner: `capblend_contour.go` derives it and
the `capblend_geom.go`/`capblend.go`/`capblend_moments.go` rows above name every
reading that carries it. Nothing here reads that term and nothing there reads
`sectionDelta`.

**Modify audit cancellation.** `Body.FilletContext`,
`Body.ChamferContext`, and `Body.ShellContext` pass one shared `workBudget`
through the pre-commit cancellation path. Fillet and Chamfer use it through the
rewrite audit; Shell also uses it for offset construction before that audit.
Every profile walk, segment pair, hole pair, and ray-boundary containment
candidate counts against it, so context cancellation is polled at phase
boundaries and at least every 256 candidates. Cancellation returns `ctx.Err()`
with the receiver still live and the recipe/document unchanged. The original
methods delegate with `context.Background()`. The `cupWall` morphology recheck
shares the same budget across profile validation and integration, offset
construction, and `auditOffsetSectionBudget` when it receives
`Document.Verify`'s context, so that path never drops cancellation.

## Context-aware placement

`Body.PlacedContext`, `Body.DuplicateContext`, and `Body.PlacedCopyContext` accept the caller's context; the existing placement and copy methods remain background-context compatibility wrappers. Rebuild and placement paths must check cancellation before committing a new body.

## Conventions

- Go style, testing and file-layout rules: `~/.claude/docs/go.md`. Tests use
  `testify/require` (never `assert`), external `_test` package, `t.Context()`.
- User-facing usage → executable Go examples in `examples/` with verified
  `// Output:` blocks. NEVER README-only snippets.
- Docs state **current state only** — no changelogs, no "was X, now Y".
- Design docs live in `docs/<topic>-design.md`.

## Verification

```
go test ./...      # must pass
go vet ./...       # must pass
golangci-lint run  # v2.12.2, config in .golangci.yml
```
